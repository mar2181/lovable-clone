# HS Web App Builder — Audit Report

**Date:** 2026-08-16T11:04Z · **Run:** #13 (scheduled, 3-day cadence) · **Mode:** auto-fix safe / flag risky
**Overall: 🔴 RED** — Two P0s persist (dashboard 404 day 69+, voice box down). P1 security bypass **RESOLVED**. Build engine likely fixed (not E2E-verifiable from cloud).

> Probe: 6 GREEN · 9 YELLOW · 2 RED | Deep audit: 6 domains | Auto-healed: none (both P0s require creds or Vercel access)

---

## 🚩 Needs Mario (ranked P0 → P3)

### P0 — Frontend routes all 404 in production [CHRONIC — day 69+]

`/dashboard`, `/editor`, `/sign-in`, `/sign-up` and every auth-gated route return HTTP 404 from Vercel. Only `/` (the landing page) returns 200.

**Evidence:** `curl https://hswebappbuilder.space/dashboard` → `404 This page could not be found` (Next.js `_not-found` page). Same for `/editor`, `/login`, `/sign-in`. The deployed build ID is `dpl_2c4Nh7oPfiEvi4zajZtvEwyobKpJ`.

**Local build:** CLEAN — `npm run build` builds all 6 routes with 0 TypeScript errors in 13s. `/dashboard` is a proper static prerendered route in the output. The code is fine; the Vercel deployment is stale or failed.

**Root cause:** Vercel is serving an old deployment that pre-dates the current `app/dashboard/` structure, or a deployment failed silently. First flagged ~2026-06-08; audits #27–#59 all confirmed. The codebase has been updated many times since but Vercel never received a successful redeploy.

**Fix (no code change needed — Vercel redeploy only):**
1. Log into vercel.com → `hswebappbuilder.space` project
2. Click "Redeploy" on the latest deployment (or trigger from master: `git commit --allow-empty -m "chore: trigger redeploy" && git push`)
3. Verify `/dashboard` → redirects to sign-in (not 404)

---

### P0 — Voice box DOWN (RunPod pod unreachable) [CHRONIC — 20+ days]

`https://udcz4k7kse1zw6-7860.proxy.runpod.net` returns 404 on ALL endpoints: `/healthz`, `/health`, `/api/status`, `/`. Pet Concierge voice features are completely dark.

**Evidence:** every probe path → HTTP 404. Previous healthy reading was `ok:true, 18 tools, voices [will, jack]` (2026-06-07 audit).

**Pet concierge wiring in code is INTACT:** `components/pet-concierge.tsx:99` sets `data-backend="selfhosted"` + `data-connect-url="/boxapi"` ✅; `next.config.ts:12` proxies `/boxapi/:path*` → `https://udcz4k7kse1zw6-7860.proxy.runpod.net/:path*` ✅. The code wiring is correct — the RunPod pod is simply off or crashed.

**Fix (needs RunPod access — not auto-fixable from cloud):**
1. `curl -H "Authorization: Bearer $RUNPOD_API_KEY" https://rest.runpod.io/v1/pods/udcz4k7kse1zw6` — check pod state
2. If `EXITED`: `POST https://rest.runpod.io/v1/pods/udcz4k7kse1zw6/start`
3. If pod running but `/healthz` still 404: `POST https://udcz4k7kse1zw6-7860.proxy.runpod.net/dashboard/agent/restart`
4. Verify: `/healthz` → `ok:true`, `tools.count >= 18`, voices present

---

### P2 — Cloud audit can no longer smoke-test build E2E [NEW — side-effect of P1 fix]

The security bypass fix (excellent!) removed the `Bearer dev-local-user` backdoor. **This breaks the cloud audit's authenticated smoke tests.** The worker returns 401 for every API call that requires auth, so this run cannot confirm build engine E2E. Worker health is GREEN but actual build quality is unverified.

**Fix (no urgency — low-cost improvement):**
Add a read-only `GET /api/health/extended` endpoint in `worker/src/index.ts` that returns OpenRouter credit balance (without the auth gate) so cloud audits can verify credits without a real Clerk token. Alternatively, provision a dedicated audit service-account token in `worker/.dev.vars` and set it in the cloud scheduler's secrets.

---

### P3 — ESLint: 157 errors (up from ~130 warnings) [WORSENED]

Previous audit noted "130+ @typescript-eslint/no-explicit-any warnings." ESLint now reports **157 errors** (not warnings). Errors include:
- `no-explicit-any` — 140+ instances across `components/` and `worker/src/routes/`
- `template-picker.tsx:80` — "Cannot access variable before it is declared" (stale-closure risk, carries forward from last audit)
- `editor.tsx:647` — "Calling setState synchronously within an effect can trigger cascading renders"
- `react/no-unescaped-entities` — unescaped apostrophe

`next build` succeeds (TypeScript 0 errors) so this doesn't block prod, but ESLint errors blocking CI is a risk if a linting gate is ever added.

---

### P3 — `middleware.ts` → `proxy.ts` rename still pending [PERSISTS]

`next build` still emits: `⚠ The "middleware" file convention is deprecated. Please use "proxy" instead.` This is a Next.js 16 deprecation. Rename before a Next 17+ upgrade breaks the Clerk auth proxy layer.

**Fix (repo edit):** `git mv middleware.ts proxy.ts` + update any imports + test Clerk redirect on `/dashboard` + redeploy.

---

## 🔧 Auto-healed this run

**None.** Both P0s require external creds (RunPod API key for pod restart, Vercel token for redeploy). Per policy.md, these are flagged — not auto-applied in cloud.

---

## ✅ Verified WORKING

| Domain | Verdict | Evidence |
|---|---|---|
| **P1 Security bypass** | 🟢 RESOLVED | `GET /api/credits` with `Bearer dev-local-user` → **401** (was `tier:unlimited`). `wrangler.toml` now has `ENVIRONMENT = "production"`; `DEV_BYPASS_AUTH` not set. Commits `d511723` + `e5fc9f5` fixed this. |
| **Worker health** | 🟢 GREEN | `/health` → `{"status":"ok"}`, `/api/spec.json` → openapi 3.1.0, `/api/projects` anon → 401 |
| **Worker auth gate** | 🟢 GREEN | Wrong token → 401, no bearer → 401, anon → 401 — all correct |
| **Worker CORS** | 🟢 GREEN | OPTIONS preflight → 204; exact-match origin allowlist in `worker/src/index.ts` (not `*`) |
| **GitHub import route** | 🟢 GREEN | `/api/github/import` anon → 401 (deployed + gated) |
| **Sandpack unit guards** | 🟢 GREEN | `test-sandpack-alias.ts`: 5/5 PASS; `test-sandpack-assets.ts`: 9/9 PASS |
| **Frontend root** | 🟢 GREEN | `https://hswebappbuilder.space/` → 200 |
| **Local build** | 🟢 GREEN | `next build` clean in 13s; TypeScript: 0 errors; 6 routes compiled (/, /dashboard, /editor/[id], /sign-in, /sign-up, /test-preview) |
| **Pet concierge wiring (code)** | 🟢 GREEN | `data-backend="selfhosted"`, `data-connect-url="/boxapi"`, `next.config.ts` `/boxapi → https://udcz4k7kse1zw6-7860.proxy.runpod.net` — chain intact in repo |
| **.dev.vars gitignored** | 🟢 GREEN | `.gitignore:53` covers `worker/.dev.vars`; `git check-ignore` confirms; file not tracked in history |
| **Build engine (code)** | 🟡 LIKELY FIXED | Commits `1ae4ce8` (Sonnet 5 default) + `eb5ab74` (32k token cap to prevent over-reservation) address the OpenRouter credits-exhaustion failure. **Not E2E verified** — dev auth properly off blocks cloud smoke test. Verify locally with a real Clerk token: `POST /api/projects` + `POST /api/build/<id>`. |

---

## 🩹 Hygiene backlog (P3 — batched)

- **Pod billed 24/7** (~$0.69/hr ≈ $500/mo) even while voice box is unreachable. Need idle-shutdown + wake-on-demand flow. Especially urgent given box has been down 20+ days.
- **Telegram/SMS not configured** — `/api/share/health` returns Unauthorized (dev bypass now off, can't check freely); flagged as dark since first audit.
- **ESLint 157 errors** (worsened from ~130 warnings) — 140+ `no-explicit-any`, cascade setState, unescaped entity.
- **middleware→proxy rename** — Next.js 16 deprecation, must rename before Next 17+.
- **`GET /api/projects/<deleted-id>` returns 200 `{"error":"..."}` instead of 404** — cosmetic but incorrect HTTP semantics.
- **15 stale audit report PRs open** (#20 Dependabot audit PRs, all draft, all unreviewed — see reconciliation). Consider closing old reports beyond the last 3.
- **Cloud smoke test gap** — now that dev bypass is fixed, build E2E can't be tested from cloud. Add a read-only health endpoint or audit service token.

---

## 🗂 GitHub Reconciliation (surface only — nothing merged)

Master HEAD: `eb5ab74` (`chat: cap AI output at 32k tokens`, Aug 10 2026).

### Non-audit branches

| Branch | PR | Age | What it is | Recommendation |
|---|---|---|---|---|
| `claude/builder-buddy-rail` | #20 (open) | 74 days | Docked Space Mario assistant rail; PR body says live pod re-pointed. | **BRING IN FOR REVIEW** — has waited 74 days since last recommended; if box comes back up, this PR becomes higher priority. If box is being retired, consider closing. |
| `dependabot/npm_and_yarn/mcp-server/npm_and_yarn-04db377a11` | #47 (open) | 26 days | `fast-uri 3.1.2 → 3.1.4` in `/mcp-server` only | **MERGE** — safe patch version bump in an isolated subdirectory, no risk |
| `dependabot/npm_and_yarn/npm_and_yarn-5984bbb696` | #28 (open) | 57 days | 2 updates across 3 directories | **REVIEW AND MERGE** — 57 days old; check for conflicts with recent master commits then merge |

### Stale audit report PRs

PRs #27, #29, #32, #33, #36, #37, #39, #42, #45, #49, #51, #53, #56, #58, #59 — all draft audit reports from Jun 19 → Aug 7, all open. **CLOSE** these 15 PRs (they're historical read-only reports, not code to merge). Consider keeping only the latest 1-2 for reference.

### Branches gone since last audit

`fix/sanitizer-icon-imports`, `archive/windows-side-*`, `backup/pr13-*` — all absent from `git ls-remote` now. Presumed deleted (correct housekeeping per prior recommendations).

---

## Δ vs last report (2026-08-07)

| Item | Direction | Detail |
|---|---|---|
| **P1 Security bypass** | ✅ **RESOLVED** | `Bearer dev-local-user` → 401. Commits `d511723`+`e5fc9f5` fixed. Was P1 for 70 days. |
| **P0 Build engine** | ✅ **LIKELY FIXED** | `1ae4ce8` Sonnet 5 + `eb5ab74` 32k cap address the OpenRouter over-reservation failure. Not E2E verified from cloud. |
| **P0 /dashboard 404** | 🔴 **PERSISTS** | Day 69+. Code is fine (local build clean). Vercel needs a redeploy. |
| **P0 Voice box DOWN** | 🔴 **PERSISTS** | All endpoints 404. Pod needs restart (requires RunPod key). |
| **Cloud smoke-test gap** | 🆕 **NEW** | Dev bypass fix is correct, but removes the cloud audit's authenticated path. Needs a read-only health endpoint or audit service token. |
| **ESLint errors** | ⬆️ **WORSENED** | Was ~130 `any` warnings; now 157 errors (configuration stricter or new code added). |
| **Dependabot PRs** | 🆕 **PENDING** | #47 (fast-uri) and #28 (2 updates) open and unmerged. |

---

## ⛔ Checks skipped for lack of cloud creds

| Check | Reason skipped | What to run locally |
|---|---|---|
| **RunPod pod-power** | `RUNPOD_API_KEY` absent | `curl -H "Authorization: Bearer $RUNPOD_API_KEY" https://rest.runpod.io/v1/pods/udcz4k7kse1zw6` |
| **Vercel deploy-drift** | `VERCEL_TOKEN` absent | `vercel ls` or check Vercel dashboard — confirm why `dpl_2c4Nh7oPfiEvi4zajZtvEwyobKpJ` is the live build |
| **Worker auto-redeploy self-heal** | `CLOUDFLARE_API_KEY`+email absent | Not needed this run (worker is healthy) |
| **Telegram notification** | No Telegram token in cloud env | `POST /api/share/phone` locally |
| **Build E2E (SSE)** | Dev auth properly off (correct!) | Use a real Clerk session token: `POST /api/projects` → `POST /api/build/<id>` — verify `build_complete` + files |
| **Box `POST /api/connect` session test** | Pod is down + HMAC token in `.dev.vars` | First restart pod; then test HMAC-gated broker locally |

---

## How this was produced

Tier-1 probe (`qa/audit/audit-probe.mjs --json`) → 6 domain deep checks → this synthesis.
Probe JSON: `qa/audit/reports/latest-probe.json`.
Probe overall: RED (2 RED · 9 YELLOW · 6 GREEN).
