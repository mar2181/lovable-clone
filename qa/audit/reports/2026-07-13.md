# HS Web App Builder — Audit Report

**Date:** 2026-07-13T11:10Z · **Run:** #12 (scheduled, 3-day cadence) · **Mode:** auto-fix safe / flag risky
**Overall: 🔴 RED** — Voice box remains down (7+ days). P1 dev-bypass persists (run #12, day 36). Build engine RESTORED (was P0 in run #11). New P2: `/dashboard` returns true 404 in prod.

> Probe: 5 GREEN · 9 YELLOW · 3 RED  |  Deep audit: 6 domains  |  Auto-healed: none (no cloud creds)

---

## 🚩 Needs Mario (ranked P0 → P3)

### P0 — Voice Box completely DOWN (7+ days) [PERSISTS from run #10]

`GET https://udcz4k7kse1zw6-7860.proxy.runpod.net/healthz` → HTTP 404. Confirmed by both probe and direct `curl`. Every voice and brain feature is non-functional for all users.

**App-side wiring is intact (no code change needed):**
- `data-backend="selfhosted"` ✅ (`components/pet-concierge.tsx:99`)
- `data-connect-url="/boxapi"` ✅ (`components/pet-concierge.tsx:100`)
- `next.config.ts /boxapi → https://udcz4k7kse1zw6-7860.proxy.runpod.net/:path*` ✅ (lines 12–13)
- `agent_id` present ✅

**Fix (operational — no repo change, but no RUNPOD_API_KEY in cloud to auto-heal):**
1. Log into RunPod dashboard → pod `udcz4k7kse1zw6` → check status (stopped? crashed?)
2. If stopped: Start pod. If crashed: Restart.
3. Verify: `curl https://udcz4k7kse1zw6-7860.proxy.runpod.net/healthz` → `{"ok":true,...}`
4. Confirm `tools.count` ≈ 18 and `tts.voices` includes `will`/`jack`

**Note — billing:** Pod billed ~$0.69/hr (~$500/mo) 24/7 with `active_sessions:0` and service not running. Resolve immediately.

---

### P1 — Anyone can be owner in prod (`Bearer dev-local-user`) [PERSISTS — run #12, day 36]

Live confirmation: `GET /api/credits` with `Authorization: Bearer dev-local-user` → `{"tier":"unlimited","balance":9999}`. Full owner-level access: project CRUD, build pipeline, all endpoints.

Root cause (`worker/wrangler.toml` lines 16–17):
```toml
ENVIRONMENT = "development"
DEV_BYPASS_AUTH = "1"
```
Auth gate in `worker/src/middleware/auth.ts:62–63`:
```ts
const devBypassEnabled =
  c.env.ENVIRONMENT === "development" || c.env.DEV_BYPASS_AUTH === "1";
```
Both vars are in `[vars]` (deployed to prod). Either one alone enables the bypass.

**Exact fix (CONFIRM-FIRST — not auto-applied per policy.md):**
1. Log into `hswebappbuilder.space` with real Clerk credentials and confirm projects visible.
2. Remove from `worker/wrangler.toml` `[vars]` block:
   ```toml
   # DELETE both lines:
   ENVIRONMENT = "development"
   DEV_BYPASS_AUTH = "1"
   ```
   These belong ONLY in `worker/.dev.vars` (already gitignored — `.gitignore:52–53` confirmed).
3. `cd worker && wrangler deploy`
4. Verify: `curl -H "Authorization: Bearer dev-local-user" .../api/credits` → 401.

---

### P2 — `/dashboard` returns true HTTP 404 in production [NEW REGRESSION]

`curl -L https://hswebappbuilder.space/dashboard` → **HTTP 404** with `age: 952965` (~11 days). This is a real 404 — not a Clerk auth redirect. The response includes Vercel's `content-disposition: inline; filename="404"` and `access-control-allow-origin: *`.

**Previous state:** Run #11 (2026-07-10) showed `/dashboard` → 307 Clerk auth redirect (correct). The 11-day CDN age indicates the 404 was first served around 2026-07-02.

**Local build is correct:** `npm run build` today produces `○ /dashboard` (static, compiled cleanly). TypeScript: 0 errors. The route exists in code — this is a Vercel deployment or CDN cache issue.

**Fix:**
1. Open Vercel dashboard for `hswebappbuilder.space` → confirm latest deployment status.
2. If stale or failed: Trigger a redeploy of master HEAD (`50a7531`).
3. If CDN cached a stale 404: Purge Vercel edge cache for `/dashboard`.
4. Verify: `curl -L https://hswebappbuilder.space/dashboard` → 307 redirect to `/sign-in` (Clerk auth).

---

### P2 — `claude/builder-buddy-rail` unmerged 39+ days, still no PR [PERSISTS]

Complete, working feature (dual-backend Space Mario voice assistant rail: `buddy-panel.tsx`, `pc-embed.js`, `pet-concierge.tsx` updates, +4,253 LOC across 5 files). Flagged "BRING IN FOR REVIEW" since run #2 (2026-06-07). Merge-conflict risk compounds with each master commit.

**Action:** Open a PR for `claude/builder-buddy-rail` against master, or explicitly archive with a decision record.

---

### P2 — `middleware.ts` → `proxy.ts` rename required [PERSISTS since run #3]

Every `npm run build` emits:
> `"The 'middleware' file convention is deprecated. Please use 'proxy' instead."`

If Next.js drops backward compat in a future patch, Clerk auth gates for `/dashboard`, `/editor/[id]`, etc. will silently drop — which may explain the current `/dashboard → 404` regression.

**Fix (repo change — flag):** `git mv middleware.ts proxy.ts`, update exports + `config` object to match proxy API, test auth gates, redeploy Vercel.

---

### P2 — `web_search` likely dead (`TAVILY_API_KEY` unset) [PERSISTS, UNCONFIRMED]

`wrangler.toml` has no `TAVILY_API_KEY` in `[vars]` or secrets. Worker code degrades gracefully with `"TAVILY_API_KEY is not configured"`.

**Fix:** Run a chat turn: `POST /api/build/<id>` with `useWebSearch:true`. If "TAVILY_API_KEY is not configured": `cd worker && wrangler secret put TAVILY_API_KEY`.

---

### P2 — `template-picker.tsx:80`: `fetchTemplates` called before declaration [PERSISTS since run #2]

ESLint confirms: `react-hooks/exhaustive-deps` warning at line 82, with `fetchTemplates` used inside `useEffect` before its declaration below. Stale-closure risk at runtime.

**File:** `components/dashboard/template-picker.tsx:80`
**Fix:** Move the `fetchTemplates` function declaration above the `useEffect` that calls it (lines 80–87), or convert to `useCallback` and include in the dependency array.

---

## 🔧 Auto-healed this run
**None.** No operational RED was fixable without cloud creds. P0 box restart requires `RUNPOD_API_KEY`. P1 requires Mario's confirm-first workflow. P2 /dashboard requires Vercel access.

---

## ✅ Verified WORKING

| Domain | Verdict | Evidence this run |
|---|---|---|
| **Build engine** | 🟢 GREEN (RESTORED) | SSE E2E confirmed: POST `/api/projects` → 201 (`bBafdJzDrh`) → POST `/api/build/<id>` → `build_complete` with 22 files (4 pages: Home/About/Services/Contact + Header/Footer + components + constants). Zero `{type:"error"}` events. Zero "Insufficient credits". Cleanup: DELETE → `{"success":true}`. Latency well within 200s. |
| **Worker infra** | 🟢 GREEN | `/health` 200, `/api/spec.json` 200 (openapi 3.1.0), `/api/projects` anon → 401 (gated). Project CRUD functional. |
| **GitHub Import** | 🟢 GREEN | `dan5py/react-vite-shadcn-ui` → **201**, 22 files imported, 1 skipped, 0 failed, 0 errored, `truncated:false`. Cleanup DELETE → 200 `{"success":true}`. Auth gate anon → 401 ✅. |
| **Sandpack unit guards** | 🟢 GREEN | Alias: **5/5 PASS** (baseUrl, ~/alias, @ui/*, vite-only, parseJsonc). Assets: **9/9 PASS** (raster, aliased raster, svg URL, /public literal, css url(), svgr ?react, code imports, no-op, svgToDataUri). |
| **Frontend HTTP root** | 🟢 GREEN | `https://hswebappbuilder.space/` → 200. `/sign-in` → 200. |
| **Frontend build** | 🟢 GREEN | `next build` (Next.js 16.2.6, Turbopack) clean — TypeScript: **0 errors** (7.5s), compiled in 9.9s. 6 routes: `/`, `/_not-found`, `/dashboard`, `/editor/[id]`, `/sign-in`, `/sign-up`, `/test-preview`. |
| **Concierge wiring** | 🟢 GREEN | `data-backend="selfhosted"` + `data-connect-url="/boxapi"` + `next.config.ts` proxy to correct pod URL — chain intact. Just need pod restart. |
| **Security — `.dev.vars`** | 🟢 GREEN | `.gitignore:52–54` covers `worker/.dev.vars`. `git check-ignore` confirms. File absent from working tree (not deployed to repo). |
| **Security — CORS** | 🟢 GREEN | `worker/src/index.ts` uses exact-match origin allowlist. No `*` wildcard. |

---

## 🩹 Hygiene backlog (P3 — batched)

- **P3 — Pod billed 24/7** (~$0.69/hr ≈ $500/mo) with `active_sessions:0` and service down. No idle-watcher / sleep controller.
- **P3 — 156 ESLint errors, 80 warnings** (236 total). Primary: `@typescript-eslint/no-explicit-any` across `worker/src/routes/`, `sdk/src/`, `mcp-server/src/`. Two auto-fixable. Stable count vs run #11.
- **P3 — MCP key `X-User-Id` spoofing** (`worker/src/middleware/auth.ts:46–52`): Any client with `MCP_API_KEY` can set arbitrary `userId` via header. Fix: ignore or allowlist the `X-User-Id` header; use fixed service identity.
- **P3 — Telegram/SMS not configured** (`/api/share/health` → `telegram:false, sms:false`). In-app sharing over phone is dark.
- **P3 — Build planner over-scoping**: Single-sentence prompt triggers 4-page build (24 files, 2 batches). Pushes into timeout territory. Add `max_pages=1` cap for single-sentence prompts.
- **P3 — Missing `useEffect` deps**: `clone-project-dialog.tsx:41` (missing `project`), `project-list.tsx:51` (missing `loadProjects`). Stale-closure risk.
- **P3 — `<img>` vs `<Image />`**: `attachment-preview.tsx:26`, `chat-message.tsx:66`. LCP/bandwidth suboptimal.
- **P3 — `GET /api/projects/<deleted-id>` → HTTP 200** with `{"error":"Project not found"}`. Should be 404. Cosmetic.
- **P3 — Missing `"type":"module"`** in `package.json` causes Node double-parse warning in QA scripts.
- **P3 — CORS preflight** leaks headers/methods to rejected origins (HTTP 204, no ACAO). Not exploitable but leaks API surface.
- **P3 — `worker/.gitignore` missing** — `.dev.vars` protection relies solely on root `.gitignore`.
- **P3 — `GARY_AGENT_ID` constant** holds Space Mario ID but named for old persona. Rename to `SPACE_MARIO_AGENT_ID`.
- **P3 — Unescaped apostrophe** `components/editor/chat-panel.tsx:263`.
- **P3 — Next.js workspace warning** — stray `/home/mario/package-lock.json` confuses Turbopack workspace root detection.

---

## 🗂 GitHub Reconciliation (surface only — nothing merged)

Master HEAD: `50a7531` (2026-06-07 audit sync) — **no code changes in 36 days.**

| Branch | Age | What it is | Recommendation | Reason |
|---|---|---|---|---|
| **`claude/builder-buddy-rail`** | 39 days | Dual-backend Space Mario voice rail (+4,253 LOC, 5 files — `buddy-panel.tsx`, updated `pet-concierge.tsx`, `pc-embed.js`, `space-mario-buddy.js`) | **OPEN PR NOW** | Complete working feature. Flagged since run #2 (2026-06-07). Conflict risk escalating daily. |
| **`dependabot/npm_and_yarn/npm_and_yarn-5984bbb696`** | 23 days | Bumps `tsx` 4.7→4.22 in `mcp-server/`, plus lock-file updates in root and `worker/` | **BRING IN** | Safe dev-dep toolchain bump; low risk. Helps close outstanding moderate advisories. |
| **`audit/report-2026-06-10` through `audit/report-2026-07-10`** (10 branches) | 3–33 days | Audit report markdown only — no product code | **ARCHIVE (delete all)** | Reports superseded by this run. **Exception:** check `audit/report-2026-06-13` for the `chore: normalize fsevents dev flag` commit — cherry-pick or drop before deleting. |

**Open PRs:** 0 on master. **Dependabot security advisories:** not checked in cloud (no `gh` auth) — last count was 10 moderate (2026-06-07). Check: `https://github.com/mar2181/lovable-clone/security/dependabot`.

---

## Δ vs previous run (`latest.md` from 2026-07-10, run #11)

| Item | Direction | Detail |
|---|---|---|
| **Build engine** | ✅ RESTORED | Was P0 broken (silent SSE close, no `build_complete`) in run #11. TODAY: full SSE E2E confirmed — `build_complete` + 22 files, zero errors. |
| **Voice box** | 🔴 PERSISTS | Day 7+ of total outage. Same `/healthz → 404`. No pod action (no `RUNPOD_API_KEY` in cloud). |
| **/dashboard HTTP** | 🔴 REGRESSED | Run #11 showed 307 Clerk redirect (correct). TODAY: true HTTP 404 with 11-day CDN age (~2026-07-02). Vercel deployment or CDN issue. |
| **P1 dev-bypass-prod** | 🔄 PERSISTS | Run #12. `wrangler.toml` lines 16–17 unchanged. |
| **P2 builder-buddy-rail** | 🔄 PERSISTS | Now 39 days unmerged. No PR opened. Growing conflict risk. |
| **Dependabot branch** | 🔄 PERSISTS | 23 days old, not brought in. |
| **Sandpack unit guards** | ✅ STABLE | 5/5 alias, 9/9 assets. PASS. |
| **Frontend build (TypeScript)** | ✅ STABLE | 0 errors. 6 routes compiled. |
| **GitHub import** | ✅ STABLE | 201, 22 files, 0 failed. |
| **middleware.ts deprecation** | 🔄 PERSISTS | Not fixed since run #3. Possible contributor to /dashboard 404. |
| **template-picker fetchTemplates** | 🔄 PERSISTS | Not fixed since run #2. |
| **web_search TAVILY** | 🔄 PERSISTS UNCONFIRMED | No TAVILY_API_KEY in wrangler.toml. Build engine restored — owner can now confirm live. |
| **hono CORS vuln GHSA-88fw-hqm2-52qc** | 🔄 PERSISTS | Still unfixed. Mitigated by config; upgrade closes it. |
| **MCP X-User-Id spoofing** | 🔄 PERSISTS | First surfaced run #11. |

---

## ⛔ Checks skipped (cloud creds absent — cover on local on-demand run)

| Check | Reason skipped | What to run locally |
|---|---|---|
| **RunPod pod-power** | `RUNPOD_API_KEY` not in cloud env | `curl -H "Authorization: Bearer $RUNPOD_API_KEY" https://rest.runpod.io/v1/pods/udcz4k7kse1zw6` |
| **Box `POST /api/connect` session** | Broker HMAC token in `.dev.vars` | Confirm `connectUrl`+token returned; test real voice session |
| **Vercel deploy-drift** | `VERCEL_TOKEN` not in cloud env | `vercel ls` — confirm master `50a7531` is deployed; purge/redeploy if stale |
| **Worker auto-redeploy self-heal** | `CLOUDFLARE_API_KEY`+email not in cloud | `node qa/audit/selfheal.mjs` — not needed (worker health GREEN) |
| **Telegram notification** | No Telegram token in cloud | `POST /api/share/phone` or `notify_mario()` |
| **web_search live test** | TAVILY key not in cloud | `POST /api/build/<id>` with `useWebSearch:true` — engine restored so this can now be tested |
| **Dependabot PR advisory count** | `gh` not authenticated in cloud | `gh pr list --repo mar2181/lovable-clone`; check security tab for counts |

---

## How this was produced

Tier-1 probe (`qa/audit/audit-probe.mjs --json`) → 6 parallel deep checks (Worker build E2E SSE, GitHub import live + unit guards, frontend build/lint, voice box wiring, security/git checks, branch reconciliation).
Probe JSON: `qa/audit/reports/latest-probe.json`.
Probe overall: RED (3 RED · 9 YELLOW · 5 GREEN).
