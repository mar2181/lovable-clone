# HS Web App Builder — Audit Report

**Date:** 2026-07-19T11:10Z · **Run:** #14 (scheduled, 3-day cadence) · **Mode:** auto-fix safe / flag risky
**Overall: 🔴 RED** — 2 P0 outages: Build engine STALLED (regression from run #13) + Voice box DOWN (day 13+). P1 dev-bypass day 42. 43 Dependabot vulns (5 HIGH). /dashboard 404 (day 17). No master commits in 42 days.

> Probe: 5 GREEN · 9 YELLOW · 3 RED  |  Deep audit: 6 domains (parallel)  |  Auto-healed: none (no cloud creds)

---

## 🚩 Needs Mario (ranked P0 → P3)

### P0 — Voice Box completely DOWN (day 13+) [PERSISTS — runs #10–14]

`GET https://udcz4k7kse1zw6-7860.proxy.runpod.net/healthz` → no response (empty body, connection reset / 404). Confirmed by both probe (`concierge/box-health = RED`) and direct curl this run.  
All voice, brain, Pet Concierge features are non-functional. Pod `udcz4k7kse1zw6` may be stopped, crashed, evicted, or billing cut.

**App-side wiring is intact (no code change needed — verified this run):**
- `data-backend="selfhosted"` ✅ (`components/pet-concierge.tsx:99`)
- `data-connect-url="/boxapi"` ✅ (`components/pet-concierge.tsx:100`)
- `next.config.ts:12–13 /boxapi → https://udcz4k7kse1zw6-7860.proxy.runpod.net/:path*` ✅

**Fix (operational — requires RunPod access, no repo change):**
1. Log into RunPod dashboard → pod `udcz4k7kse1zw6` → check status.
2. If stopped → Start. If crashed → Restart. If evicted/deleted → Re-create.
3. Verify: `curl https://udcz4k7kse1zw6-7860.proxy.runpod.net/healthz` → `{"ok":true,...}`
4. Confirm `tools.count` ≈ 18 and `tts.voices` includes `will` / `jack`.

**Cost note:** Pod billed ~$0.69/hr 24/7 with `active_sessions:0` and service dead for 13+ days. ~$210 burned with zero value since it went down. Fix or delete it now.

---

### P1 — Anyone can be owner in prod (`Bearer dev-local-user`) [PERSISTS — day 42]

Live confirmation this run: `GET /api/credits` with `Authorization: Bearer dev-local-user` → HTTP 200 `{"tier":"unlimited","balance":9999}`.

Root cause: `worker/wrangler.toml` lines 16–17 in `[vars]`:
```toml
ENVIRONMENT = "development"
DEV_BYPASS_AUTH = "1"
```
Auth gate in `worker/src/middleware/auth.ts:62–63`:
```ts
const devBypassEnabled =
  c.env.ENVIRONMENT === "development" || c.env.DEV_BYPASS_AUTH === "1";
```
Both vars deployed to production Cloudflare Worker via `[vars]`. Either one enables bypass.

**Fix (CONFIRM-FIRST per policy.md — not auto-applied):**
1. Log into `hswebappbuilder.space` with real Clerk credentials → confirm your projects are visible.
2. Edit `worker/wrangler.toml`, remove from `[vars]`:
   ```toml
   # DELETE both lines:
   ENVIRONMENT = "development"
   DEV_BYPASS_AUTH = "1"
   ```
   These belong only in `worker/.dev.vars` (already gitignored — `.gitignore:53` confirmed this run).
3. `cd worker && wrangler deploy`
4. Verify: `curl -H "Authorization: Bearer dev-local-user" .../api/credits` → 401.

---

### P1 — 43 Dependabot vulnerabilities on master (5 HIGH) [PERSISTS — run #14]

Advisory count unchanged at **43 total — 5 HIGH, 30 moderate, 8 low** (same as runs #11–13).  
Dependabot PR #28 (`dependabot/npm_and_yarn/npm_and_yarn-5984bbb696` — `tsx` 4.7→4.22 bump, safe toolchain-only) has been open **29 days** without review.

**Fix:**
1. Check: `https://github.com/mar2181/lovable-clone/security/dependabot`
2. Merge PR #28 first (safe lock-file-only toolchain bump; low risk; closes some moderate advisories).
3. Accept remaining Dependabot PRs for lock-file-only bumps.
4. Review any HIGH-severity advisory requiring API changes manually.

---

### P2 — `/dashboard` → 404 in prod (Clerk protect-rewrite) [PERSISTS — day 17]

`GET https://hswebappbuilder.space/dashboard` → HTTP 404. Response headers this run:
```
x-clerk-auth-reason: protect-rewrite, session-token-and-uat-missing
x-clerk-auth-status: signed-out
x-matched-path: /404
x-vercel-cache: HIT
age: 86356                            ← CDN serving stale 404 (~24 hrs cached)
last-modified: Sat, 18 Jul 2026 11:07:05 GMT
```
`/` → 200 ✅  `next build` confirms `/dashboard` builds fine as a static route (✓ Generating static pages). The 404 is a runtime Clerk middleware issue: unauthenticated requests should redirect to `/sign-in`, not rewrite to the 404 page.

Likely cause: the deprecated `middleware.ts` → `proxy.ts` rename (warned again in this run's `npm run build`) may break Clerk's middleware registration in Next.js 16.

**Fix (two steps — do both):**
1. **Purge Vercel CDN:** Trigger a forced redeploy of master `50a7531` from Vercel dashboard to bust CDN cache.
2. **Rename middleware file (repo edit):** `git mv middleware.ts proxy.ts`, update export/config to Next.js proxy API, test Clerk auth gate, redeploy.
3. Verify: `curl -I https://hswebappbuilder.space/dashboard` → 307 redirect to `/sign-in`.

---

### P0 — Build Engine STALLED (regression from run #13) [NEW THIS RUN]

**SSE E2E test result:** Build starts but hangs. POST `/api/build/vTHP1lQeAQ` with `{"description":"A simple landing page with a hero and one button."}` → SSE stream opened, received initial events, then **zero further events for 200 seconds**. Hard timeout hit. Project cleaned up (`DELETE` → 200 ✅).

Full SSE output before stall:
```
data: {"type":"build_start","totalPages":4,"totalBatches":2}
data: {"type":"batch_start","batchIndex":0,"totalBatches":2,"pages":["Home","About Us","Services"]}
data: {"type":"page_status","page":"Home","status":"generating"}
data: {"type":"page_status","page":"About Us","status":"generating"}
data: {"type":"page_status","page":"Services","status":"generating"}
[200s silence — no page_complete, no build_complete, no error]
```

**No `{"type":"error"}` events** (so likely not "Insufficient credits" — but cannot rule out silent OpenRouter timeout). The LLM generation call for batch 0 pages fires but never resolves. Worker's wall-clock / CPU limit may be killing the upstream API call silently.

**Fix options:**
1. **Check OpenRouter credits**: `GET https://openrouter.ai/api/v1/auth/key` with OpenRouter API key → confirm `usage_remaining` > 0. If 0: top up at openrouter.ai.
2. **Check worker CPU/wall-clock**: Cloudflare Workers default to 30s CPU time. If the LLM call takes >30s, it's killed with no error event emitted. Consider streaming the response from OpenRouter (if not already doing so) or switching to a faster model.
3. **Test smaller prompt**: `POST /api/build/<id>` with `{"description":"Hello","max_pages":1}` — if a 1-page build also stalls, it confirms a global infra problem; if it succeeds, the planner's 4-page default is hitting the limit.

Last confirmed GREEN: run #13 (2026-07-16) — `build_complete` + 22 files, zero errors.

---

### P2 — `claude/builder-buddy-rail` unmerged 45 days [ESCALATING]

Complete feature branch (dual-backend Space Mario voice rail: `buddy-panel.tsx`, `pc-embed.js`, updated `pet-concierge.tsx`) with +4,253 LOC across 5 files. PR #20 is open but unreviewed. Flagged "BRING IN FOR REVIEW" since run #2 (2026-06-07). No action in 45 days.

**Fix (choose one):**
- Review and merge PR #20 (the branch is ready per PR body).
- Explicitly archive: close PR #20 + `git push origin :claude/builder-buddy-rail`.

---

## ✅ Verified WORKING this run

| Domain | Verdict | Evidence |
|---|---|---|
| **Worker infra** | 🟢 GREEN | `/health` 200, `/api/spec.json` 200 (openapi 3.1.0), `/api/projects` anon → 401 (correct). |
| **GitHub Import** | 🟢 GREEN | `POST /api/github/import` → 201, 22 files imported, 0 failed. Cleanup DELETE → 200. |
| **Sandpack unit guards** | 🟢 GREEN | `test-sandpack-alias.ts`: 5/5 PASS. `test-sandpack-assets.ts`: 9/9 PASS. |
| **Frontend HTTP** | 🟢 GREEN (partial) | `https://hswebappbuilder.space/` → 200 ✅. `/dashboard` → 404 ❌ (see P2 above). |
| **Frontend build** | 🟢 GREEN | `next build`: 0 TypeScript errors, 6 routes built in 12.6s. Turbopack clean. |
| **Concierge wiring** | 🟢 GREEN | `data-backend="selfhosted"` + `/boxapi` proxy chain intact. Just needs pod restart. |
| **Security — `.dev.vars`** | 🟢 GREEN | `.gitignore:53` covers `worker/.dev.vars`. `git check-ignore -v` confirms. Not tracked. |
| **Security — CORS** | 🟢 GREEN | `worker/src/index.ts` exact-match origin allowlist. `credentials:true` correct. |

---

## 🩹 Hygiene backlog (P3 — batched)

Items unchanged from previous runs unless noted:
- **Pod billed 24/7** (~$0.69/hr) with `active_sessions:0` and service dead 13+ days. ~$210 wasted. Idle-watcher / sleep controller still absent.
- **156 ESLint errors, 80 warnings** (236 total). Primary: `@typescript-eslint/no-explicit-any` in `worker/src/routes/`, `sdk/src/`, `mcp-server/src/`. 4 new lint errors this run: `image-gen.ts:81`, `video-gen.ts:139,177,217`. Stable count overall.
- **middleware.ts deprecation** (Next.js 16 warns `proxy` rename needed) — persists, now implicated in the `/dashboard` 404.
- **MCP key `X-User-Id` spoofing** (`worker/src/middleware/auth.ts:46–52`): any caller with `MCP_API_KEY` can set arbitrary `userId` via header.
- **Telegram/SMS not configured** (`/api/share/health` → `{"ok":true,"telegram":false,"sms":false}`). In-app phone sharing dark.
- **Build planner over-scoping**: every prompt triggers 4-page build (22+ files, 180–240s). Consider `max_pages=1` for single-sentence prompts.
- **Missing `useEffect` deps**: `clone-project-dialog.tsx:41`, `project-list.tsx:51` — stale-closure risk.
- **`<img>` vs `<Image />`**: `attachment-preview.tsx:26`, `chat-message.tsx:66`.
- **`GET /api/projects/<deleted-id>`** → HTTP 200 `{"error":"Project not found"}`. Should be 404.
- **Missing `"type":"module"`** in `package.json` — causes Node double-parse warning in QA scripts.
- **`worker/.gitignore` absent** — `.dev.vars` protection relies solely on root `.gitignore`.
- **`GARY_AGENT_ID`** constant — rename to `SPACE_MARIO_AGENT_ID`.
- **Next.js workspace warning** — stray `/home/mario/package-lock.json` confuses Turbopack.
- **10 open audit-report PRs** (#27, #29, #32, #33, #36, #37, #39, #42 + any new) — stale, can be closed once reports are read.

---

## 🗂 GitHub Reconciliation (surface only — nothing merged)

Master HEAD: `50a7531` (2026-06-07) — **no new code commits in 42 days.**

| Branch | Age | What it is | Recommendation | Reason |
|---|---|---|---|---|
| **`claude/builder-buddy-rail`** (PR #20) | 45 days | Dual-backend Space Mario voice rail | **OPEN PR → REVIEW OR ARCHIVE** | Flagged 7 consecutive runs. Conflict risk is real. Longest-waiting item. |
| **`dependabot/npm_and_yarn/npm_and_yarn-5984bbb696`** (PR #28) | 29 days | `tsx` 4.7→4.22 in mcp-server + lock files | **MERGE** | Safe toolchain-only bump. 29 days idle. |
| **`audit/report-2026-06-10` through `audit/report-2026-07-16`** (12 branches) | 3–39 days | Audit report markdown only | **ARCHIVE (delete all)** | Superseded by this run. Reports are in PRs; PRs can be closed too. |
| **`master`** | — | Production HEAD | — | |

**Open PRs (all open):** #20 (builder-buddy-rail), #27–#42 series (audit reports), #28 (Dependabot).  
**Security advisories:** 43 total (5 HIGH, 30 moderate, 8 low) — **unchanged since run #11**.

---

## Δ vs previous run (run #13, 2026-07-16)

| Item | Direction | Detail |
|---|---|---|
| **Voice box** | 🔴 PERSISTS | Day 13+ (was day 10+ in run #13). `/healthz` → no response. |
| **P1 dev-bypass-prod** | 🔴 PERSISTS | Day 42 (was day 39). `wrangler.toml:16–17` unchanged. |
| **/dashboard HTTP** | 🔴 PERSISTS | Day 17 (was day 14). CDN age:86356 — Vercel caching stale 404. |
| **Dependabot 43 vulns** | 🔴 PERSISTS | 5 HIGH unchanged. PR #28 still unreviewed (29 days). |
| **Build engine** | 🔴 REGRESSED | SSE stall at 200s: `build_start`+`batch_start`+3× `page_status` then nothing. No `build_complete`. No `{"type":"error"}`. New regression (was GREEN run #13). |
| **GitHub import** | ✅ STABLE | 201, 22 files, 0 failed, cleanup 200. |
| **Sandpack unit guards** | ✅ STABLE | 5/5 + 9/9 PASS. |
| **Frontend build** | ✅ STABLE | 0 TS errors, 6 routes, 12.6s. |
| **master HEAD** | ⚠️ STALE | `50a7531` — 42 days without a commit. |
| **builder-buddy-rail PR #20** | 🔴 PERSISTS | 45 days unmerged (was 42 in run #13). |
| **ESLint errors** | ➕ MINOR INCREASE | 156 errors (was stable). `video-gen.ts` added 3 `any` errors this run. |
| **web_search TAVILY** | 🔄 UNCONFIRMED | Build engine confirmed up last run — test live: POST /api/chat with web_search intent. |

---

## ⛔ Checks skipped (cloud creds absent)

| Check | Reason skipped | What to run locally |
|---|---|---|
| **RunPod pod-power** | `RUNPOD_API_KEY` absent | `curl -H "Authorization: Bearer $RUNPOD_API_KEY" https://rest.runpod.io/v1/pods/udcz4k7kse1zw6` |
| **Box `POST /api/connect` session** | Broker HMAC token in `.dev.vars` | Confirm `connectUrl`+token returned; test voice session |
| **Vercel deploy-drift** | `VERCEL_TOKEN` absent | `vercel ls` — confirm master `50a7531` deployed; force redeploy to bust CDN 404 |
| **Worker auto-redeploy self-heal** | `CLOUDFLARE_API_KEY`+email absent | Not needed (worker GREEN); `node qa/audit/selfheal.mjs` when worker goes down |
| **Telegram notification** | No Telegram token | `POST /api/share/phone` or `notify_mario()` locally |
| **web_search live test** | TAVILY key absent | `POST /api/chat` with web_search intent; if "unavailable": `wrangler secret put TAVILY_API_KEY` |

---

## How this was produced

Tier-1 probe (`qa/audit/audit-probe.mjs --json`) → 6 parallel deep checks: Worker build SSE E2E (background agent), GitHub import live + unit guards (PASS), frontend HTTP + build + lint, voice box healthz, security/git checks, branch reconciliation via GitHub MCP.  
Probe JSON: `qa/audit/reports/latest-probe.json`.  
Probe overall: RED (3 RED · 9 YELLOW · 5 GREEN).
