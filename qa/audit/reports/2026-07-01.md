# HS Web App Builder — Audit Report

**Date:** 2026-07-01T11:15Z · **Run:** #5 (scheduled, 3-day cadence) · **Mode:** auto-fix safe / flag risky
**Overall: 🔴 RED** — 3 outages (build engine, dashboard/editor, voice box). P1 security bypass persists for 4th consecutive audit.

> Probe: 5 GREEN · 9 YELLOW · 3 RED  |  Deep audit: 6 domains  |  Auto-healed: none (no cloud creds for operational fixes)

---

## 🚩 Needs Mario (ranked P0 → P3)

### P0-A — Build Engine BROKEN (REGRESSION — was RESTORED in #4) ‼️

**SSE E2E failed on two successive attempts:**

**Attempt 1:** `POST /api/build/tghVQYtl45` — `build_start` (4 pages, 2 batches) → `batch_stream` content received → all 3 pages → `error` → **`{"type":"error","error":"Batch 1: AI returned no files","batchIndex":0}`**

**Attempt 2:** `POST /api/build/NqRJq6HkVY` — `build_start` → `page_status generating`×3 → **no further events after 180s timeout** (no `build_complete`, no `error`)

The batch_stream in attempt 1 shows the AI did begin generating (`{"files":{"/src/App.tsx":"...`), but the worker's file-extraction parser returned empty — almost certainly because OpenRouter **truncated the response mid-stream** (credits exhausted or quota throttled). The second attempt produced no AI output at all after 180s.

**Impact: TOTAL product outage.** Users cannot build any app.

**Fix (needs OpenRouter dashboard):**
1. Check OpenRouter credit balance at https://openrouter.ai/account — if zero, top up.
2. If balance is present, check `worker/src/services/build-orchestrator.ts` for JSON parsing regressions (the "no files" error path).
3. After replenishment: re-run `POST /api/build` and confirm `build_complete` with files.
4. Consider adding a keepalive SSE ping every 30s to prevent gateway timeouts on long builds.

---

### P0-B — Vercel Deploy Drift: `/dashboard` and `/editor/[id]` → 404 (REGRESSION — was GREEN in #4)

`GET https://hswebappbuilder.space/dashboard` → **HTTP 404** (Vercel serving `x-vercel-cache: HIT` — stale cached deployment `dpl_BmNaxdmTmqKQmHeWVKcR6cqESRMp`).

`GET https://hswebappbuilder.space/editor` → **HTTP 404**.

`GET https://hswebappbuilder.space/sign-in` → 200 (Clerk-authenticated routes still work).

Local `npm run build` shows both routes exist in code:
```
○ /dashboard      (static)
ƒ /editor/[id]   (dynamic)
```

Git HEAD is `50a7531` but Vercel's deployed `dpl_` predates recent commits (at least 4 commits undeployed: `50a7531`, `4af6841`, `d25c6bb`, `86e84ea`).

**Impact:** The entire app shell is inaccessible to real users — after sign-in they land on 404.

**Fix (needs Vercel dashboard or CLI):**
1. Trigger a new Vercel deployment: `vercel --prod` (or push to master if Vercel auto-deploys).
2. Verify deploy reaches READY state for commit `50a7531` or later.
3. Re-test: `curl https://hswebappbuilder.space/dashboard` → expect 200 or redirect-to-sign-in.

---

### P0-C — Voice Box DOWN (REGRESSION — was GREEN in #4)

`GET https://udcz4k7kse1zw6-7860.proxy.runpod.net/healthz` → **HTTP 404** (was `ok:true`, 18 tools, voices `[will, jack]` in last audit).

`GET .../dashboard/tools` → **HTTP 404**.

RunPod pod `udcz4k7kse1zw6` is either stopped or the proxy URL is stale. **RUNPOD_API_KEY absent in cloud — cannot verify pod power state or auto-restart.**

Code wiring is correct (no repo change needed): `components/pet-concierge.tsx` → `data-backend="selfhosted"`, `data-connect-url="/boxapi"` → `next.config.ts` → `https://udcz4k7kse1zw6-7860.proxy.runpod.net`. Chain is coherent; problem is infrastructure.

**Impact:** Voice assistant / Pet Concierge is completely offline.

**Fix (needs RunPod API key — flag, not auto-fix):**
```bash
# Check pod state
curl -H "Authorization: Bearer $RUNPOD_API_KEY" \
  https://rest.runpod.io/v1/pods/udcz4k7kse1zw6

# If EXITED/stopped:
curl -X POST -H "Authorization: Bearer $RUNPOD_API_KEY" \
  https://rest.runpod.io/v1/pods/udcz4k7kse1zw6/start

# If running but box unreachable, try:
curl -X POST https://udcz4k7kse1zw6-7860.proxy.runpod.net/dashboard/agent/restart
```
After recovery, re-check `/healthz` → `ok:true` and `tools.count` ≥ 18.

---

### P1 — Dev-bypass in prod (PERSISTS — 4th consecutive audit)

`GET /api/credits` with `Authorization: Bearer dev-local-user` → `{"tier":"unlimited","balance":9999}` (owner-level, HTTP 200).
`GET /api/credits` without auth → 401 (correct).

`worker/wrangler.toml` lines 16–17 still deploy into production `[vars]`:
```toml
ENVIRONMENT = "development"
DEV_BYPASS_AUTH = "1"
```

**Fix (CONFIRM-FIRST — policy.md):**
1. Verify ownership: log into https://hswebappbuilder.space with real Clerk credentials, confirm projects visible.
2. Remove lines 16–17 from `worker/wrangler.toml [vars]`.
3. `cd worker && wrangler deploy`.
4. Test: `Bearer dev-local-user` → expect 401 from `/api/credits`.

---

## ✅ Verified WORKING

| Domain | Verdict | Evidence |
|---|---|---|
| **Worker infra** | 🟢 GREEN | `/health` 200, `/api/spec.json` 200 (openapi 3.1.0), `/api/projects` anon → 401 (gated). Project CRUD functional (create 201, delete 200). |
| **Frontend root** | 🟢 GREEN | `https://hswebappbuilder.space/` → 200. `/sign-in` → 200. |
| **Frontend build (local)** | 🟢 GREEN | `npm run build`: Next.js 16.2.6 Turbopack, compiled in 10.3s, TypeScript 0 errors (7.4s). 6 routes generated. |
| **GitHub Import route** | 🟢 GREEN | `/api/github/import` anon → 401 (gated). Live import of `dan5py/react-vite-shadcn-ui` → **201**, 22 files, 0 failed, 0 errored. Cleanup 200 ✅ |
| **Sandpack unit guards** | 🟢 GREEN | alias: 5/5 PASS · assets: 9/9 PASS |
| **Security — .dev.vars** | 🟢 GREEN | `.gitignore:53` covers `worker/.dev.vars`. `git ls-files` → empty. Not committed. |
| **Pet Concierge wiring (code)** | 🟢 GREEN | `pet-concierge.tsx:99` `data-backend="selfhosted"`, `data-connect-url="/boxapi"`. `next.config.ts:13` → `udcz4k7kse1zw6-7860.proxy.runpod.net`. Chain coherent. Box itself is down (infra, not wiring). |

---

## 🩹 Hygiene backlog (P3 — batched)

- **ESLint: 236 problems** (156 errors, 80 warnings) — unchanged from #4. Dominated by `@typescript-eslint/no-explicit-any` in worker routes and components. Non-blocking.
- **Next.js middleware deprecation** — `npm run build` still emits: *"The 'middleware' file convention is deprecated. Please use 'proxy' instead."* Fix: `git mv middleware.ts proxy.ts` + update imports.
- **Telegram/SMS not configured** — `/api/share/health` → `{telegram:false, sms:false}`.
- **RunPod pod billed 24/7** — even when stopped, a stopped pod may retain volume cost. Check billing.
- **`worker/.dev.vars` gitignore probe YELLOW** — `git check-ignore -v` confirms `.gitignore:53` covers it but the probe flags YELLOW because the pattern is not file-name-specific. Low risk, confirmed safe.
- **`GET /api/projects/<deleted-id>`** → 200 with `{"error":"Project not found"}` instead of 404.
- **P2 — `web_search` likely still dead** (`TAVILY_API_KEY` not in `wrangler.toml` vars) — unverifiable while build engine is down.
- **P2 — `template-picker.tsx:80` hook ordering** — `fetchTemplates` used before declaration (from previous report; ESLint still flagging, runtime staleness risk).

---

## 🗂 GitHub Reconciliation (surface only — nothing merged)

| Branch | Age | What it changes | Recommendation | Reason |
|---|---|---|---|---|
| **`claude/builder-buddy-rail`** | ~27 days | Docked Space Mario assistant rail (`buddy-panel.tsx`, `pc-embed.js`), re-pointed to live pod endpoint | **BRING IN FOR REVIEW** (unchanged rec from #4) | Live pod re-pointed 2026-06-04; functional. Awaiting human review. |
| **`dependabot/npm_and_yarn/npm_and_yarn-5984bbb696`** | NEW | Bumps 2 packages across 3 directories (lock-file changes) | **REVIEW + MERGE if tests pass** | Security/patch bump; automatic. No functional code change expected. |
| **`audit/report-*`** (6 branches) | 3–21 days | Audit reports only | **ARCHIVE** (delete) once report branch for this run is merged | Pure metadata; no code changes. |

Branches from previous audit now absent (resolved): `fix/sanitizer-icon-imports`, `archive/windows-side-2026-05-11`, `backup/pr13-pre-resolve-2026-05-12` — all archived or merged. ✅

---

## Δ vs last report (2026-06-07 / audit #4)

| Item | Direction | Detail |
|---|---|---|
| Build engine | 🔴 REGRESSED | Was RESTORED (build_complete + 22 files). Now: "AI returned no files" + timeout. OpenRouter credits likely exhausted again. |
| `/dashboard` + `/editor` | 🔴 REGRESSED | Were 200. Now 404. Vercel deployment stale (≥4 commits undeployed). |
| Voice box | 🔴 REGRESSED | Was `ok:true`, 18 tools. Now `/healthz` → 404. Pod likely stopped. |
| P1 dev-bypass-prod | 🔴 PERSISTS (4th audit) | `ENVIRONMENT=development` + `DEV_BYPASS_AUTH=1` still in `[vars]`. |
| GitHub import | ✅ STILL OK | 201, 22 files, cleanup 200. |
| Sandpack unit guards | ✅ STILL OK | 5/5 alias, 9/9 assets. |
| Frontend local build | ✅ STILL OK | TypeScript 0 errors, 6 routes. |
| `fix/sanitizer-icon-imports` | ✅ RESOLVED | Branch gone (merged or archived). |
| Archive/backup branches | ✅ RESOLVED | Both deleted since last audit. |
| Dependabot bump | 🆕 NEW | `dependabot/npm_and_yarn/npm_and_yarn-5984bbb696` — 2 packages across 3 dirs. Review needed. |

---

## ⛔ Checks skipped — lack of cloud creds

| Check | Reason skipped | Local command |
|---|---|---|
| **RunPod pod-power + restart** | `RUNPOD_API_KEY` absent | `curl -H "Authorization: Bearer $RUNPOD_API_KEY" https://rest.runpod.io/v1/pods/udcz4k7kse1zw6` |
| **Vercel deploy-drift (confirm SHA)** | `VERCEL_TOKEN` absent | `vercel ls` / Vercel dashboard → confirm `50a7531` not deployed |
| **Worker auto-redeploy self-heal** | `CLOUDFLARE_API_KEY`+email absent | `cd worker && wrangler deploy` (not needed this run — worker is up) |
| **Telegram notification** | Telegram token absent | `notify_mario()` / `POST /api/share/phone` |
| **Box `POST /api/connect` session** | RunPod broker token absent | Test HMAC-gated broker locally |
| **OpenRouter credit balance** | No OpenRouter API key | Check https://openrouter.ai/account |

---

## How this was produced

Tier-1 probe (`qa/audit/audit-probe.mjs --json`) + 6 deep domain checks (HTTP, SSE build E2E ×2, GitHub import live test, Sandpack unit guards, wiring inspection, security confirm, frontend build + lint). Probe JSON: `qa/audit/reports/latest-probe.json`.
Probe overall: **RED** (5 GREEN · 9 YELLOW · 3 RED).
