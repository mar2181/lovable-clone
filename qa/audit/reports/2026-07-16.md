# HS Web App Builder — Audit Report

**Date:** 2026-07-16T11:10Z · **Run:** #13 (scheduled, 3-day cadence) · **Mode:** auto-fix safe / flag risky
**Overall: 🔴 RED** — Voice box P0 (10+ days down). /dashboard 404 persists (day 14). P1 dev-bypass on day 39. Build engine GREEN. No new master commits in 39 days.

> Probe: 5 GREEN · 9 YELLOW · 3 RED  |  Deep audit: 6 domains  |  Auto-healed: none (no cloud creds)

---

## 🚩 Needs Mario (ranked P0 → P3)

### P0 — Voice Box completely DOWN (10+ days) [PERSISTS — run #13, runs #10–12]

`GET https://udcz4k7kse1zw6-7860.proxy.runpod.net/healthz` → HTTP 404 (both `/healthz` and `/health`). Confirmed by both probe and direct curl. Every voice and brain feature is non-functional for all users. Pod may be stopped, crashed, or billing has been cut.

**App-side wiring is intact (no code change needed — verified this run):**
- `data-backend="selfhosted"` ✅ (`components/pet-concierge.tsx:99`)
- `data-connect-url="/boxapi"` ✅ (`components/pet-concierge.tsx:100`)
- `next.config.ts /boxapi → https://udcz4k7kse1zw6-7860.proxy.runpod.net/:path*` ✅ (lines 12–13)
- `agent_id` present ✅

**Fix (operational — no repo change, but RUNPOD_API_KEY absent in cloud to auto-heal):**
1. Log into RunPod dashboard → pod `udcz4k7kse1zw6` → check status (stopped? crashed? evicted?)
2. If stopped: Start pod. If crashed: Restart. If deleted: Re-create.
3. Verify: `curl https://udcz4k7kse1zw6-7860.proxy.runpod.net/healthz` → `{"ok":true,...}`
4. Confirm `tools.count` ≈ 18 and `tts.voices` includes `will` / `jack`

**Cost note:** Pod billed ~$0.69/hr (~$500/mo) 24/7 with `active_sessions:0` and service down for 10+ days. That's ~$170 burned with zero service. Fix or delete the pod.

---

### P1 — Anyone can be owner in prod (`Bearer dev-local-user`) [PERSISTS — run #13, day 39]

Live confirmation this run: `GET /api/credits` with `Authorization: Bearer dev-local-user` → `{"tier":"unlimited","balance":9999}`. Full owner-level access to all endpoints: project CRUD, build pipeline, file access.

Root cause (`worker/wrangler.toml` lines 16–17, CONFIRMED unchanged):
```toml
ENVIRONMENT = "development"
DEV_BYPASS_AUTH = "1"
```
Auth gate in `worker/src/middleware/auth.ts:62–63`:
```ts
const devBypassEnabled =
  c.env.ENVIRONMENT === "development" || c.env.DEV_BYPASS_AUTH === "1";
```
Both vars are in `[vars]` (deployed to production Cloudflare Worker). Either one alone enables the bypass.

**Exact fix (CONFIRM-FIRST — not auto-applied per policy.md):**
1. Log into `hswebappbuilder.space` with real Clerk credentials and confirm your projects are visible.
2. Remove from `worker/wrangler.toml` `[vars]` block:
   ```toml
   # DELETE both lines:
   ENVIRONMENT = "development"
   DEV_BYPASS_AUTH = "1"
   ```
   These belong ONLY in `worker/.dev.vars` (already gitignored — `.gitignore:53` confirmed safe this run).
3. `cd worker && wrangler deploy`
4. Verify: `curl -H "Authorization: Bearer dev-local-user" .../api/credits` → 401.

---

### P1 — 43 Dependabot vulnerabilities on master (5 HIGH) [PERSISTS — run #13]

GitHub advisory count: **43 total — 5 HIGH, 30 moderate, 8 low** (unchanged from run #12).
The 5 HIGH advisories may include RCE or auth-bypass vectors in transitive dependencies.

**Fix:**
1. Check: `https://github.com/mar2181/lovable-clone/security/dependabot`
2. Merge `dependabot/npm_and_yarn/npm_and_yarn-5984bbb696` first (safe `tsx` 4.7→4.22 toolchain bump).
3. Accept remaining Dependabot PRs for lock-file-only bumps.
4. Review any HIGH-severity advisory that requires API changes manually.

---

### P2 — `/dashboard` → 404 in prod (Clerk protect-rewrite) [PERSISTS — day 14]

`GET https://hswebappbuilder.space/dashboard` → HTTP 404 with:
- `x-clerk-auth-reason: protect-rewrite, session-token-and-uat-missing`
- `x-clerk-auth-status: signed-out`
- `x-matched-path: /404`
- `x-vercel-cache: HIT` (Vercel cache serving stale 404)

`/sign-in` → 200 (sign-in page exists and is reachable). `/` → 200. Root is healthy.

When unauthenticated users hit `/dashboard`, Clerk's `auth.protect()` is rewriting to the 404 page instead of redirecting to `/sign-in`. This may be caused by:
- The deprecated `middleware.ts` → `proxy.ts` rename not yet applied (Next.js 16 deprecation warning); Clerk middleware may not be fully integrated in the legacy file.
- Stale Vercel CDN edge cache on `/dashboard` serving a cached 404 response.

**Fix (two steps — do BOTH):**
1. **Purge Vercel edge cache:** Trigger a forced redeploy of master HEAD (`50a7531`) from the Vercel dashboard to bust the CDN cache.
2. **Rename middleware file (repo change — flag):** `git mv middleware.ts proxy.ts`, update the export name/config to match Next.js proxy API, test Clerk auth gates, redeploy Vercel. This resolves both the deprecation warning and potential Clerk-middleware regression.
3. Verify: `curl -I https://hswebappbuilder.space/dashboard` → 307 redirect to `/sign-in` (not 404).

---

### P2 — `claude/builder-buddy-rail` unmerged 42+ days [ESCALATING — open PR or archive]

Complete, working feature (dual-backend Space Mario voice assistant rail: `buddy-panel.tsx`, `pc-embed.js`, `pet-concierge.tsx` updates, +4,253 LOC across 5 files). Flagged "BRING IN FOR REVIEW" since run #2 (2026-06-07). No PR opened. Merge-conflict risk compounds daily with no master movement. This branch will diverge further if any master commit lands.

**Action (choose one):** Open a draft PR for `claude/builder-buddy-rail` → master, OR explicitly archive with a decision record (`git push origin :claude/builder-buddy-rail`).

---

### P2 — `middleware.ts` → `proxy.ts` rename required [PERSISTS since run #3]

Every `npm run build` emits:
> `"The 'middleware' file convention is deprecated. Please use 'proxy' instead."`

Next.js 16 deprecation warning. If Next.js drops backward compat in a patch, Clerk auth gates for `/dashboard`, `/editor/[id]`, etc. will silently fail. Possibly contributing to current `/dashboard → 404` regression.

**Fix (repo change — flag):** `git mv middleware.ts proxy.ts`, update exports and `config` object to match proxy API, test auth gates, redeploy Vercel.

---

### P2 — `web_search` likely dead (`TAVILY_API_KEY` unset) [PERSISTS UNCONFIRMED — run #13]

`wrangler.toml` has no `TAVILY_API_KEY` in `[vars]` or listed under secrets. Worker degrades gracefully with `"TAVILY_API_KEY is not configured"`. Build engine is now healthy — this can finally be tested live.

**Fix:** Run a chat turn: `POST /api/build/<id>` with `useWebSearch:true`. If "TAVILY_API_KEY is not configured": `cd worker && wrangler secret put TAVILY_API_KEY`.

---

### P2 — `template-picker.tsx:80`: `fetchTemplates` called before declaration [PERSISTS since run #2]

ESLint: `fetchTemplates` used inside `useEffect` before declaration below — stale-closure risk at runtime.

**File:** `components/dashboard/template-picker.tsx:80`
**Fix:** Move `fetchTemplates` declaration above the `useEffect` that calls it, or convert to `useCallback`.

---

## 🔧 Auto-healed this run
**None.** No operational RED was fixable without cloud creds. P0 box restart requires `RUNPOD_API_KEY`. P1 requires Mario's confirm-first workflow. P2 /dashboard requires Vercel dashboard access.

---

## ✅ Verified WORKING

| Domain | Verdict | Evidence this run |
|---|---|---|
| **Build engine** | 🟢 GREEN | SSE E2E confirmed: POST `/api/projects` → 201 → POST `/api/build/<id>` → `build_complete` with **22 files** (4 pages: Home/About/Services/Contact + Header/Footer + UI components). Zero `{type:"error"}`. Zero "Insufficient credits". Cleanup: DELETE → `{"success":true}`. **Note: requires 200–240s window** — 190s timeout insufficient for 4-page build. |
| **Worker infra** | 🟢 GREEN | `/health` 200, `/api/spec.json` 200 (openapi 3.1.0), `/api/projects` anon → 401 (gated). CRUD functional. |
| **GitHub Import** | 🟢 GREEN | `dan5py/react-vite-shadcn-ui` → **201**, 22 files imported, 1 skipped, 0 failed, 0 errored, `truncated:false`. Cleanup DELETE → 200 ✅. Auth gate anon → 401 ✅. |
| **Sandpack unit guards** | 🟢 GREEN | Alias: **5/5 PASS** (baseUrl, ~/alias, @ui/*, vite-only, parseJsonc). Assets: **9/9 PASS** (raster, aliased raster, svg URL, /public literal, css url(), svgr ?react, code imports, no-op, svgToDataUri). |
| **Frontend root** | 🟢 GREEN | `https://hswebappbuilder.space/` → 200. `/sign-in` → 200. |
| **Concierge wiring** | 🟢 GREEN | `data-backend="selfhosted"` + `data-connect-url="/boxapi"` + `next.config.ts` → RunPod pod URL — chain intact in repo. Just needs pod restart. |
| **Security — `.dev.vars`** | 🟢 GREEN | `.gitignore:53` covers `worker/.dev.vars`. `git check-ignore -v` confirms. `git ls-files worker/.dev.vars` → empty (not tracked). |
| **Security — CORS** | 🟢 GREEN | `worker/src/index.ts` exact-match origin allowlist (not `*`). `credentials:true` correct. |

---

## 🩹 Hygiene backlog (P3 — batched, unchanged from run #12)

- **Pod billed 24/7** (~$0.69/hr ≈ $500/mo) with `active_sessions:0` and service down 10+ days. ~$170 burned with zero value. No idle-watcher / sleep controller.
- **156 ESLint errors, 80 warnings** (236 total). Primary: `@typescript-eslint/no-explicit-any` across `worker/src/routes/`, `sdk/src/`, `mcp-server/src/`. Stable count.
- **MCP key `X-User-Id` spoofing** (`worker/src/middleware/auth.ts:46–52`): Any caller with `MCP_API_KEY` can set arbitrary `userId` via header. Fix: ignore/allowlist this header; use fixed service identity.
- **Telegram/SMS not configured** (`/api/share/health` → `telegram:false, sms:false`). In-app sharing over phone is dark.
- **Build planner over-scoping**: Every prompt triggers 4-page build (22+ files, 2 batches). Even single-sentence prompts hit 200–240s. Add `max_pages=1` cap for single-sentence prompts.
- **Missing `useEffect` deps**: `clone-project-dialog.tsx:41` (missing `project`), `project-list.tsx:51` (missing `loadProjects`). Stale-closure risk.
- **`<img>` vs `<Image />`**: `attachment-preview.tsx:26`, `chat-message.tsx:66`. LCP/bandwidth suboptimal.
- **`GET /api/projects/<deleted-id>`** → HTTP 200 with `{"error":"Project not found"}`. Should be 404.
- **Missing `"type":"module"`** in `package.json` causes Node double-parse warning in QA scripts.
- **CORS preflight** leaks headers/methods to rejected origins (HTTP 204, no ACAO). Not exploitable but leaks API surface.
- **`worker/.gitignore` missing** — `.dev.vars` protection relies solely on root `.gitignore`.
- **`GARY_AGENT_ID` constant** holds Space Mario ID but named for old persona. Rename to `SPACE_MARIO_AGENT_ID`.
- **Unescaped apostrophe** `components/editor/chat-panel.tsx:263`.
- **Next.js workspace warning** — stray `/home/mario/package-lock.json` confuses Turbopack workspace root detection.

---

## 🗂 GitHub Reconciliation (surface only — nothing merged)

Master HEAD: `50a7531` (2026-06-07 audit sync) — **no code changes in 39 days.**

| Branch | Age | What it is | Recommendation | Reason |
|---|---|---|---|---|
| **`claude/builder-buddy-rail`** | 42 days | Dual-backend Space Mario voice rail (+4,253 LOC: `buddy-panel.tsx`, `pc-embed.js`, updated `pet-concierge.tsx`, `space-mario-buddy.js`) | **OPEN PR OR ARCHIVE** | Complete feature, flagged 6 runs. Conflict risk escalating daily with no action. |
| **`dependabot/npm_and_yarn/npm_and_yarn-5984bbb696`** | 26 days | Bumps `tsx` 4.7→4.22 in `mcp-server/`, lock-file updates in root + `worker/` | **BRING IN** | Safe dev-dep toolchain bump; low risk. Helps close moderate Dependabot advisories. |
| **`audit/report-2026-06-10` through `audit/report-2026-07-13`** (11 branches) | 3–36 days | Audit report markdown only — no product code | **ARCHIVE (delete all)** | Reports superseded by this run. Exception: verify `audit/report-2026-06-13` for `chore: normalize fsevents dev flag` commit before deleting. |

**Open PRs:** 0 on master.
**Dependabot security advisories:** 43 total — **5 HIGH, 30 moderate, 8 low** (unchanged from run #12).
See: `https://github.com/mar2181/lovable-clone/security/dependabot`

---

## Δ vs previous run (run #12, 2026-07-13)

| Item | Direction | Detail |
|---|---|---|
| **Voice box** | 🔴 PERSISTS | Day 10+ of total outage. `/healthz → 404` and `/health → 404`. Day 7+ in run #12. |
| **P1 dev-bypass-prod** | 🔄 PERSISTS | Day 39. `wrangler.toml` lines 16–17 unchanged. |
| **/dashboard HTTP** | 🔴 PERSISTS | Same Clerk protect-rewrite → 404. CDN HIT still serving stale 404. Day 14 since first detected (~2026-07-02). |
| **Dependabot 43 vulns** | 🔄 PERSISTS | 5 HIGH unchanged. `dependabot/npm_and_yarn-5984bbb696` not merged. |
| **Build engine** | ✅ STABLE | SSE E2E confirmed again — `build_complete` + 22 files. Zero errors. |
| **GitHub import** | ✅ STABLE | 201, 22 files, 0 failed. |
| **Sandpack unit guards** | ✅ STABLE | 5/5 + 9/9 PASS. |
| **claude/builder-buddy-rail** | 🔄 PERSISTS | Now 42 days unmerged. No PR opened. |
| **middleware.ts deprecation** | 🔄 PERSISTS | Not fixed since run #3. Likely contributing to /dashboard 404. |
| **web_search TAVILY** | 🔄 PERSISTS UNCONFIRMED | Build engine up — owner can now test live. |
| **MCP X-User-Id spoofing** | 🔄 PERSISTS | First surfaced run #11. Still unfixed. |
| **master HEAD** | ⚠️ STALE | `50a7531` — no new commits in 39 days (since 2026-06-07). |

---

## ⛔ Checks skipped (cloud creds absent — cover on local on-demand run)

| Check | Reason skipped | What to run locally |
|---|---|---|
| **RunPod pod-power** | `RUNPOD_API_KEY` not in cloud env | `curl -H "Authorization: Bearer $RUNPOD_API_KEY" https://rest.runpod.io/v1/pods/udcz4k7kse1zw6` |
| **Box `POST /api/connect` session** | Broker HMAC token in `.dev.vars` | Confirm `connectUrl`+token returned; test real voice session |
| **Vercel deploy-drift** | `VERCEL_TOKEN` not in cloud env | `vercel ls` — confirm master `50a7531` is deployed; purge CDN + redeploy if stale |
| **Worker auto-redeploy self-heal** | `CLOUDFLARE_API_KEY`+email not in cloud | `node qa/audit/selfheal.mjs` — not needed (worker health GREEN) |
| **Telegram notification** | No Telegram token in cloud | `POST /api/share/phone` or `notify_mario()` |
| **web_search live test** | TAVILY key not in cloud | `POST /api/build/<id>` with `useWebSearch:true` — build engine restored so this can now be tested |
| **Frontend build (`npm run build`)** | `node_modules` absent in cloud clone | `npm ci && npm run build` locally — previous run #12 confirmed 0 TS errors, 6 routes |
| **Dependabot PR count** | `gh` not authenticated in cloud | `gh pr list --repo mar2181/lovable-clone`; check security/dependabot tab |

---

## How this was produced

Tier-1 probe (`qa/audit/audit-probe.mjs --json`) → 6 parallel deep checks: Worker build E2E SSE (confirmed `build_complete` + 22 files with 240s window), GitHub import live + unit guards, frontend HTTP, voice box healthz, security/git checks, branch reconciliation.
Probe JSON: `qa/audit/reports/latest-probe.json`.
Probe overall: RED (3 RED · 9 YELLOW · 5 GREEN).
