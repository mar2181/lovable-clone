# HS Web App Builder — Audit Report

**Date:** 2026-07-22T11:30Z · **Run:** #3 (scheduled, 3-day cadence) · **Mode:** auto-fix safe / flag risky  
**Overall: 🔴 RED** — 3 P0s active (build engine silent-hang, dashboard 404, voice box down). P1 security bypass persists for third consecutive audit.

> Probe: 5 GREEN · 9 YELLOW · 3 RED  |  Deep audit: 6 domains  |  Auto-healed: none (cloud run — no creds)

---

## 🚩 Needs Mario (ranked P0 → P3)

### P0-A — Build engine silently hangs — never reaches `build_complete` [REGRESSION]

**Evidence:** Three independent SSE builds (`/api/build/<id>`) across this audit run all exhibit the same pattern:

```
[T+0s] build_start {"totalPages":4,"totalBatches":2}
[T+0s] batch_start {"batchIndex":0,"pages":["Home","About Us","Services"]}
[T+0s] page_status Home/About Us/Services → "generating"
[T+200s] <stream closes cleanly — no batch_stream, no page_complete, no build_complete, no error>
```

Zero `{"type":"error"}` events. Zero "Insufficient credits" messages. The SSE connection stays open for 200 seconds then closes silently (server-side). This means the worker dispatches the request to OpenRouter but OpenRouter never streams tokens back. The default model is `moonshotai/kimi-k2.6` (set in `worker/src/routes/build.ts` line 52).

**Likely cause:** `moonshotai/kimi-k2.6` is unavailable or silently timing out on OpenRouter — either the model is overloaded/withdrawn, or OpenRouter is holding the connection open without generating output. Distinct from the June credits exhaustion (which emitted `{"type":"error","message":"Insufficient credits"}`).

**Confirmed working:** Project CRUD (create/delete), credit gate passes for dev-local-user (isOwner resolves correctly through `registerOwnerIfAdmin`), SSE plumbing alive. The failure is specifically at the LLM generation step.

**Fix (flag — needs Mario):**
1. Log into the OpenRouter dashboard and confirm `moonshotai/kimi-k2.6` model status. Check if it's rate-limited or withdrawn.
2. If model is unavailable: change the default in `worker/src/routes/build.ts` line 52 from `"moonshotai/kimi-k2.6"` to a reliably available model (`anthropic/claude-haiku-4-5` or `openai/gpt-4o-mini`).
3. Deploy: `cd worker && wrangler deploy`
4. Verify: POST /api/build → SSE → `build_complete` with files.

---

### P0-B — `/dashboard` → 404 in production [REGRESSION since last audit]

**Evidence:** `curl -s -o /dev/null -w "%{http_code}" https://hswebappbuilder.space/dashboard` → **404** with full Next.js 404 page body. Was **200** in the June 7 audit.

**Route confirmed present** locally: `app/dashboard/page.tsx` + `app/dashboard/layout.tsx` exist. Not a missing file.

**Root cause (high confidence):** The recent auth commits — particularly `448b9e8` (`feat(auth): owner-pin by Clerk subject id (OWNER_CLERK_SUB)`) and `4af6841` (`fix(auth): harden builder auth`) — likely introduced new Clerk/environment-variable dependencies in `middleware.ts`. If `CLERK_SECRET_KEY` (or the new `OWNER_CLERK_SUB`) is missing from Vercel's Production environment, the Clerk middleware crashes and Vercel caches the resulting 500 as a 404. The published Clerk key is live (`pk_live_Y2xlcmsuaHN3ZWJhcHBidWlsZGVyLnNwYWNlJA`), indicating a server-side variable is missing.

**Fix (flag — needs Vercel dashboard access):**
1. Vercel → Project → Settings → Environment Variables → Production:
   - Confirm `CLERK_SECRET_KEY` starts with `sk_live_`
   - Confirm `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY` matches the `pk_live_` key above
   - Confirm `NEXT_PUBLIC_CLERK_SIGN_IN_URL` = `/sign-in` and `NEXT_PUBLIC_CLERK_SIGN_UP_URL` = `/sign-up`
   - Add `OWNER_CLERK_SUB` if the commit `448b9e8` requires it (check worker `wrangler.toml` `[vars]` for the env var name)
2. Trigger a fresh Vercel production redeploy (dashboard → Deployments → Redeploy).
3. Verify: `curl -s -o /dev/null -w "%{http_code}" https://hswebappbuilder.space/dashboard` → 307 (redirect to `/sign-in`), not 404.

---

### P0-C — Voice Box (RunPod pod) completely down [REGRESSION since last audit]

**Evidence:** All three probe endpoints on the box return CF-proxied 404 with 0-byte body:
```
GET https://udcz4k7kse1zw6-7860.proxy.runpod.net/healthz → 404 (0 bytes)
GET https://udcz4k7kse1zw6-7860.proxy.runpod.net/ → 404 (0 bytes)
GET https://udcz4k7kse1zw6-7860.proxy.runpod.net/dashboard → 404 (0 bytes)
```
`content-length: 0` + Cloudflare headers = RunPod proxy sentinel for "pod stopped/terminated". TLS handshake succeeds (the proxy is alive; the pod behind it is not).

**Wiring verified coherent** (not the cause): `components/pet-concierge.tsx` has `data-backend="selfhosted"`, `data-connect-url="/boxapi"`, `agent_id` present. `next.config.ts` proxies `/boxapi/*` → `https://udcz4k7kse1zw6-7860.proxy.runpod.net/:path*` (exact match). Code is fine.

**Fix (flag — needs RunPod dashboard or `RUNPOD_API_KEY`):**
1. Go to RunPod console → pod `udcz4k7kse1zw6`.
2. If **stopped**: click Start. Wait for Running + port 7860 exposed.
3. If **terminated**: reprovision a new pod with the Chatterbox stack (Will voice + faster-whisper + OpenRouter). Note the new pod ID and port.
4. If pod ID changed: update `next.config.ts` proxy destination host + redeploy to Vercel.
5. Verify recovery: `curl -I https://udcz4k7kse1zw6-7860.proxy.runpod.net/healthz` → 200.

---

### P1 — Dev bypass active in production (`Bearer dev-local-user`) [PERSISTS — 3rd consecutive audit]

**Live confirmation:**
```
GET /api/credits → {"tier":"unlimited","balance":9999}   (no Clerk JWT, just the dev header)
```

**Root cause:** `worker/wrangler.toml` lines 16–17 under `[vars]`:
```toml
ENVIRONMENT = "development"
DEV_BYPASS_AUTH = "1"
```
`[vars]` deploys to the live Cloudflare Worker. The bypass condition in `worker/src/middleware/auth.ts` lines 65–78 fires because both flags are true in production.

**Fix (CONFIRM-FIRST — per policy.md):**
1. Verify ownership first: log into `hswebappbuilder.space` with real Clerk credentials, confirm projects still show.
2. Remove both lines from `[vars]` in `worker/wrangler.toml`. These belong only in `worker/.dev.vars` (already gitignored — confirmed safe at `.gitignore:53`).
3. `cd worker && wrangler deploy`
4. Verify: `GET /api/credits` with `Bearer dev-local-user` → 401.

---

### P2 — `middleware.ts` must be renamed `proxy.ts` for Next.js 16+ [PERSISTS]

`/home/user/lovable-clone/middleware.ts` — Next.js 16 removed the `middleware` file convention. This file is silently ignored on the next major upgrade, removing all auth protection. The recent auth commits (`4af6841`, `448b9e8`) add to this file without addressing the rename — widening the risk.

**Fix (repo edit):** `git mv middleware.ts proxy.ts` (no imports to update — Next.js autodiscovers this file), then verify auth routes still intercept, then Vercel redeploy.

---

### P2 — `template-picker.tsx:80` — `fetchTemplates` called before declaration [PERSISTS]

`/home/user/lovable-clone/components/dashboard/template-picker.tsx` line ~80: a `useEffect` calls `fetchTemplates()` before the function is declared (~line 88). Stale-closure risk. ESLint `react-hooks/exhaustive-deps` would flag this.

**Fix:** Move `fetchTemplates` declaration above the `useEffect`, or wrap in `useCallback`.

---

### P3 — `fix/sanitizer-icon-imports` dropped without merge [NEW]

This branch was flagged **REBASE THEN REVIEW** in the June 7 audit as a real bug fix for sanitizer icon imports. It has since disappeared from the remote without any corresponding merge commit on master. The underlying bug (template icon import crash / "Home is not defined" red-screen) may still be present.

**Action:** Check if the sanitizer fix is still needed. Run the app and load a template with icon imports. If broken, recover the fix from the June 7 audit report's branch description or re-implement.

---

## ✅ Verified WORKING

| Domain | Verdict | Evidence this run |
|---|---|---|
| **Worker infra** | 🟢 GREEN | `/health` 200, `/api/spec.json` 200 (openapi 3.1.0), `/api/projects` anon → 401. |
| **Project CRUD** | 🟢 GREEN | Create → 201, Delete → 200 `{"success":true}`. ID generation working. |
| **GitHub Import** | 🟢 GREEN | Both Sandpack unit guards PASS (5/5 alias · 9/9 asset). Live import `dan5py/react-vite-shadcn-ui` → 201, 22 files, 0 failed. Cleanup 200. |
| **Frontend root** | 🟢 GREEN | `https://hswebappbuilder.space/` → 200. |
| **Security — .dev.vars** | 🟢 GREEN | `.gitignore:53` covers `worker/.dev.vars`; `git ls-files worker/.dev.vars` → empty (not tracked). |
| **Security — CORS** | 🟢 GREEN | `worker/src/index.ts` uses exact-match origin allowlist (not `*`). |
| **Worker integrations route** | 🟡 YELLOW | `telegram:false, sms:false` — unchanged, not a regression. |
| **Credits gate for real users** | 🟢 GREEN | `getOrInitCredits` seeds FREE_CREDITS=10 on first use; `fe25e08` closes the "never deducted" exploit. |

---

## 🩹 Hygiene backlog (P3 — batched)

- **Pod billed 24/7** at ~$0.69/hr while stopped (RunPod still charges stopped pods). Restart or terminate to stop billing if pod is idle.
- **Telegram/SMS not configured** (`/api/share/health` → `telegram:false, sms:false`). Phone sharing dark.
- **ESLint ~1,052 pre-existing errors** — not CI-gated. Known tech debt.
- **`reactStrictMode: false`** in `next.config.ts` suppresses double-render bug detection. Flip after fixing P2 stale-closure above.
- **`package.json` missing `"type":"module"`** — Node emits `MODULE_TYPELESS_PACKAGE_JSON` warning on `.ts` test files. Non-blocking.
- **deploy/drift** — not verified (no `VERCEL_TOKEN` in cloud). Verify locally: confirm HEAD `082650c` deployed on Vercel.
- **pod-power** — not checked (no `RUNPOD_API_KEY` in cloud). Check RunPod dashboard.
- **Voice box: no auto-fallback** if pod dies (ElevenLabs quota-dead). HA/uptime improvement needed.
- **`GET /api/projects/<deleted-id>`** returns 200 `{"error":"..."}` — should be 404. Cosmetic.

---

## 🗂 GitHub Reconciliation (surface only — nothing merged)

Master HEAD: `082650c` (P4 isolation — gate asset upload on project ownership, 2026-07-21).

| Branch | Age | Recommendation |
|---|---|---|
| **`claude/builder-buddy-rail`** | 48 days since last commit | **ESCALATE / DECIDE NOW**: 45+ days since "BRING IN" was recommended, 3rd consecutive flag. Master diverged significantly (P2/P3/P4 auth changes). Either merge via PR or close the branch — continuing to let it drift makes future integration harder. |
| **`dependabot/npm_and_yarn/mcp-server/npm_and_yarn-04db377a11`** | 1 day | **MERGE PROMPTLY** — fresh `fast-uri` bump in mcp-server. Low risk. |
| **`dependabot/npm_and_yarn/npm_and_yarn-5984bbb696`** | 32 days | **REVIEW BEFORE MERGING** — may be superseded by the `5cef645` "clear all 44 dependabot alerts" commit. Verify no overlap then merge or close. |
| **`audit/report-2026-06-10` through `audit/report-2026-07-19`** (13 branches) | 3–42 days | **PRUNE**: These are read-only audit snapshot branches accumulating every 3 days. Recommend keeping the 2 most recent and deleting the rest. Command: `git push origin --delete audit/report-2026-06-{10,13,19,22,25,28} audit/report-2026-07-{01,04,07,10,13,16}` |

**`fix/sanitizer-icon-imports`** — GONE (see P3 finding above, was previously REBASE THEN REVIEW).  
**`archive/windows-side-2026-05-11`**, **`backup/pr13-pre-resolve-2026-05-12`** — Confirmed deleted. Good.

---

## Δ vs 2026-06-07 (previous report)

| Item | Direction | Detail |
|---|---|---|
| Build engine | 🔴 REGRESSED | Was GREEN (22 files, build_complete). Now P0 silent-hang at LLM generation step. Likely `kimi-k2.6` model issue on OpenRouter. |
| Frontend /dashboard | 🔴 REGRESSED | Was GREEN (200). Now P0 (404). Likely Clerk env vars missing in Vercel after recent auth commits. |
| Voice Box | 🔴 REGRESSED | Was GREEN (ok:true, 18 tools, will/jack voices). Now P0 (pod stopped). |
| P1 dev-bypass-prod | 🔴 PERSISTS | 3rd consecutive audit with no action. |
| P2 middleware.ts rename | 🔴 PERSISTS | 2nd consecutive audit. |
| P2 template-picker.tsx:80 | 🔴 PERSISTS | 2nd consecutive audit. |
| P2 web_search/TAVILY | ⚠️ UNVERIFIABLE | Build engine down prevents confirmation. Assume still unset. |
| Credit metering exploit | ✅ RESOLVED | `fe25e08` — new users now correctly metered (FREE_CREDITS=10, seed-on-first-use). |
| Dependabot 10 advisories | ✅ RESOLVED | `5cef645` "clear all 44 dependabot alerts" + 1 new Dependabot PR today. |
| fix/sanitizer-icon-imports | ⚠️ DROPPED | Branch gone without merge — underlying bug may still exist. |
| claude/builder-buddy-rail | ⚠️ STALE | No action since 3rd flag. Now 48 days old; master has diverged. |
| audit/report branches | ℹ️ NEW PATTERN | 13 audit branches accumulating — recommend pruning. |

---

## ⛔ Checks skipped for lack of cloud creds

| Check | Reason skipped | What to run locally |
|---|---|---|
| **RunPod pod-power** | `RUNPOD_API_KEY` not in cloud | `curl -H "Authorization: Bearer $RUNPOD_API_KEY" https://rest.runpod.io/v1/pods/udcz4k7kse1zw6` |
| **Vercel deploy-drift** | `VERCEL_TOKEN` not in cloud | `vercel ls` — confirm HEAD `082650c` is deployed |
| **Worker auto-redeploy self-heal** | `CLOUDFLARE_API_KEY`+email not in cloud | Not needed this run (worker HTTP healthy) |
| **Telegram notification** | No Telegram token in cloud | `POST /api/share/phone` or `notify_mario()` — local run sends it |
| **Box `POST /api/connect` session test** | RunPod broker token in `.dev.vars` | Test HMAC-gated broker locally: confirm `connectUrl`+token after pod restart |
| **Frontend `npm run build` typecheck** | `node_modules` not installed in cloud checkout | `npm ci && npm run build` locally — confirm 0 TS errors |

---

## How this was produced

Tier-1 probe (`qa/audit/audit-probe.mjs --json`) → 6 parallel domain agents + direct SSE E2E tests (3 build attempts) → this synthesis.  
Probe JSON: `qa/audit/reports/latest-probe.json`.  
Probe overall: RED (3 RED · 9 YELLOW · 5 GREEN).
