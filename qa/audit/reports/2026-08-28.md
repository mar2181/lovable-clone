# HS Web App Builder — Audit Report

**Date:** 2026-08-28T11:05Z · **Run:** #10 (scheduled, 3-day cadence) · **Mode:** auto-fix safe / flag risky  
**Overall: 🔴 RED** — Voice box still down (P0, day 52+). Dashboard 404 in production (P2, day 79+). Build E2E unverifiable from cloud. P1 security bypass fully RESOLVED since last run. Vulns reduced 59→12.

> Probe: 6 GREEN · 9 YELLOW · 2 RED  |  Deep audit: 5 domains  |  Auto-healed: none (no cloud creds for pod restart)

---

## 🚩 Needs Mario (ranked P0 → P3)

### P0 — Voice Box DOWN (`/healthz → 404`) — Day 52+

Pod `udcz4k7kse1zw6-7860` is unreachable — `/healthz → 404`. Total voice outage. Pet Concierge frontend wiring is **correct** (data-backend=selfhosted, /boxapi proxy in `next.config.ts` points to `https://udcz4k7kse1zw6-7860.proxy.runpod.net`, exact match). This is a pure infrastructure failure — code is fine.

**This has been RED every run since at least 2026-07-07 (52+ days). Action needed.**

**Fix (operational — auto-fixable per policy.md, requires RUNPOD_API_KEY from vault):**
1. `curl -H "Authorization: Bearer $RUNPOD_API_KEY" https://rest.runpod.io/v1/pods/udcz4k7kse1zw6`
2. If EXITED/stopped: `POST https://rest.runpod.io/v1/pods/udcz4k7kse1zw6/start`
3. If running but box mute: `POST https://udcz4k7kse1zw6-7860.proxy.runpod.net/dashboard/agent/restart`
4. Verify: `GET /healthz → ok:true`

> ⚠️ Pod billed ~$0.69/hr 24/7 even when dead. If voice is not in active use, stop the pod and only restart on demand.

---

### P2 — `/dashboard` 404 in production (Vercel deploy drift) — Day 79+

`https://hswebappbuilder.space/dashboard` returns Next.js's `_not-found` page (HTTP 404). The route exists in the codebase (`app/dashboard/page.tsx`) and **local `npm run build` confirms it as a static route** — so this is deploy drift, not a code bug. Vercel is serving a stale deployment that pre-dates when the dashboard route was added.

**This has been RED every run since at least 2026-06-19 (79+ days). Action needed.**

**Fix:**
1. `vercel --prod` (or Vercel dashboard → redeploy the latest master commit `eb5ab74`).
2. Verify: `GET https://hswebappbuilder.space/dashboard → 200`

---

### P2 — Build engine E2E unverifiable from cloud (methodology gap)

The security fix (ENVIRONMENT=production, DEV_BYPASS_AUTH removed) correctly disabled `Bearer dev-local-user` in prod (returns 401 — confirmed, good). But the cloud audit can no longer run authenticated build/import E2E. Worker is reachable (`/health → 200`, `/api/spec.json → 200`, `/api/projects anon → 401` correct), but the core SSE build pipeline cannot be confirmed without a valid Clerk JWT or a dedicated audit key.

**Fix (one-time setup — eliminates this blind spot permanently):**
1. `cd worker && wrangler secret put AUDIT_API_KEY` (generate a random secret, store in vault)
2. Add an auth path in `worker/src/middleware/auth.ts` that accepts `Authorization: Bearer $AUDIT_API_KEY` and grants owner-level access for build testing.
3. Store the key in the cloud run environment so future audit runs can test E2E.

Until then: **run `node qa/audit/audit-probe.mjs --deep` locally after each deploy.**

---

### P2 — npm vulnerabilities: 12 remaining (7 high, 5 moderate) + 2 Dependabot PRs open

`npm audit` in the root workspace shows:  
`{ info: 0, low: 0, moderate: 5, high: 7, critical: 0, total: 12 }`

Two Dependabot PRs have been open and unmerged for weeks:
- **PR #28** — `dependabot/npm_and_yarn/npm_and_yarn-5984bbb696` (open since 2026-06-20, 69 days) — bumps 2 packages across 3 workspace directories
- **PR #47** — `dependabot/npm_and_yarn/mcp-server/npm_and_yarn-04db377a11` (open since 2026-07-21, 38 days) — bumps `fast-uri` in mcp-server

**Fix:** Review both PRs. If they are lock-file-only upgrades (likely), merge them. Then re-run `npm audit` to confirm vuln count drops.

---

### P3 — 25 stale open audit-report PRs cluttering GitHub

There are 25 open `audit/report-*` PRs (PR #27 through #65 excluding the 2 Dependabot PRs and PR #20). None have been merged or closed. The PR list is unusable for real work. The reports in those branches have served their notification purpose.

**Fix:**
```bash
# Close all stale audit PRs in batch (or via GitHub UI: filter by "audit/report" head branch → close)
# Then delete the branches:
git branch -r | grep 'origin/audit/report' | sed 's|origin/||' | xargs -I{} git push origin --delete {}
```
Keep only this run's branch open (`audit/report-2026-08-28`) until it's acknowledged.

---

### P3 — `middleware.ts` → `proxy.ts` rename needed (persists, multiple runs)

`npm run build` emits a deprecation warning: the `middleware` file convention is deprecated in Next.js 16+. Will break the proxy layer on the next major Next.js upgrade.

**Fix (repo edit — flagged per policy.md):**
```bash
git mv middleware.ts proxy.ts  # update any imports referencing it
npm run build  # verify proxy routes still work
```

---

### P3 — `claude/builder-buddy-rail` PR #20 (87 days open, no action)

PR #20 docks the Space Mario assistant rail in the builder UI. It was "BRING IN FOR REVIEW" in the 2026-06-07 baseline. It is still open with no reviewer assigned after 87 days.

**Fix:** Review PR #20 — merge if it's still in scope, or close/archive if superseded.

---

## ✅ Verified WORKING

| Domain | Verdict | Evidence this run |
|---|---|---|
| **Worker infra** | 🟢 GREEN | `/health → 200`, `/api/spec.json → 200` (openapi 3.1.0), `/api/projects anon → 401` (correct) |
| **Security — dev bypass** | 🟢 GREEN RESOLVED | `Bearer dev-local-user` → **401** in prod. `ENVIRONMENT=production` confirmed in `wrangler.toml`. Bypass fully off. |
| **Security — .dev.vars** | 🟢 GREEN | `.gitignore:53` covers `worker/.dev.vars`; `git ls-files worker/.dev.vars` → empty (not tracked) |
| **Frontend root** | 🟢 GREEN | `https://hswebappbuilder.space/` → 200 |
| **Frontend build** | 🟢 GREEN | `npm run build` clean — 6 routes built including `/dashboard` as static. TypeScript: 0 errors. |
| **GitHub Import route** | 🟢 GREEN | `/api/github/import anon → 401` (deployed + gated) |
| **Sandpack unit guards** | 🟢 GREEN | `test-sandpack-alias.ts`: **5/5 PASS**; `test-sandpack-assets.ts`: **9/9 PASS** |
| **Pet Concierge wiring** | 🟢 GREEN (code) | `data-backend="selfhosted"`, `data-connect-url="/boxapi"`, `next.config.ts /boxapi → https://udcz4k7kse1zw6-7860.proxy.runpod.net` (exact match). Code correct; box itself is down. |
| **Vuln reduction** | 🟢 IMPROVED | `5cef645` cleared 44 dependabot alerts. Count: 59 → 12 (7H / 5M). No criticals. |

---

## 🩹 Hygiene backlog (P3 — batched)

- **Pod billed 24/7** at ~$0.69/hr ≈ $500/mo. `active_sessions:0`. No idle-shutdown. Consider stop-on-idle + on-demand broker-wake.
- **Telegram/SMS not configured** (`/api/share/health → telegram=undefined, sms=undefined`). In-app phone sharing is dark.
- **Build engine E2E blind** in cloud (AUDIT_API_KEY not set — see P2 above).
- **`GET /api/projects/<deleted-id>`** returns HTTP 200 with `{"error":"Project not found"}` — should be 404.
- **ESLint warnings** (carried from prior run): 80+ warnings including `@typescript-eslint/no-explicit-any`, unused vars, missing `useEffect` deps, `<img>` vs `<Image />`.
- **`template-picker.tsx:80`** — `fetchTemplates` before declaration (stale-closure risk, lint violation).
- **Next.js workspace warning** — stray `/home/mario/package-lock.json` confuses Turbopack workspace root detection.

---

## 🗂 GitHub Reconciliation (surface only — nothing merged)

**Master HEAD:** `eb5ab74` (chat: cap AI output at 32k tokens)

| Branch | Age | What it is | Recommendation |
|---|---|---|---|
| **`claude/builder-buddy-rail`** (PR #20) | 87 days | Docked Space Mario assistant rail | **REVIEW** — merge or close (87 days stale) |
| **`dependabot/npm_and_yarn/npm_and_yarn-5984bbb696`** (PR #28) | 69 days | Root workspace dep bump | **MERGE** — lock-file bump, clears vulns |
| **`dependabot/npm_and_yarn/mcp-server/npm_and_yarn-04db377a11`** (PR #47) | 38 days | fast-uri bump in mcp-server | **MERGE** — safe security bump |
| **`audit/report-*`** (PRs #27–#65, 25 PRs) | 3–80 days | Scheduled audit reports | **CLOSE ALL** — served their notification purpose; clutter the PR list |

---

## Δ vs 2026-08-25 (previous run)

| Item | Direction | Detail |
|---|---|---|
| **P1 dev-bypass** | ✅ RESOLVED | Confirmed fully off in prod (401). `ENVIRONMENT=production` in wrangler.toml. |
| **Vuln count** | ✅ IMPROVED | 59 → 12 (7H / 5M). `5cef645` cleared 44 alerts between runs. |
| **Builder quality** | ✅ NEW | Sonnet 5 default + house-style design system (`1ae4ce8`). |
| **Domain routes** | ✅ NEW | Phase B.1 connect-your-own-domain API (`d782023`). |
| **AI token cap** | ✅ NEW | 32k output cap (`eb5ab74`) prevents runaway build costs. |
| **Voice box P0** | 🔴 PERSISTS | Day 52+. No change. Needs RUNPOD_API_KEY to fix. |
| **Dashboard 404** | 🔴 PERSISTS | Day 79+. No change. Needs `vercel --prod`. |
| **Build E2E** | 🔴 PERSISTS | Unverifiable from cloud. Methodology gap unchanged. |
| **Stale audit PRs** | 🔴 WORSENED | 22 → 25 open stale PRs (3 new this run). |
| **Sandpack guards** | ✅ PASS | 5/5 + 9/9 (consistent with prior runs) |

---

## ⛔ Checks skipped (lack of cloud creds)

| Check | Reason skipped | What to run locally |
|---|---|---|
| **RunPod pod-power** | `RUNPOD_API_KEY` not in cloud env | `curl -H "Authorization: Bearer $RUNPOD_API_KEY" https://rest.runpod.io/v1/pods/udcz4k7kse1zw6` |
| **Vercel deploy-drift** | `VERCEL_TOKEN` not in cloud env | `vercel ls` or Vercel dashboard — confirm `eb5ab74` is deployed |
| **Build SSE E2E** | No valid Clerk JWT or AUDIT_API_KEY in cloud | `node qa/audit/audit-probe.mjs --deep` locally, or add AUDIT_API_KEY (see P2) |
| **GitHub Import live render** | No auth to create/delete real projects | `node qa/verify-import-render.mjs <id>` locally |
| **Box POST /api/connect session** | HMAC broker token in `.dev.vars` | Test locally with RunPod key |
| **Worker auto-redeploy self-heal** | `CLOUDFLARE_API_KEY`+email not in cloud | Not needed this run (worker is up) |
| **Telegram notification** | No Telegram token in cloud | `notify_mario()` / `POST /api/share/phone` — local run sends it |

---

## How this was produced

Tier-1 probe (`qa/audit/audit-probe.mjs --json`) → sequential deep audit (cloud, no parallel agents available) → this synthesis.  
Probe JSON: see `qa/audit/reports/latest-probe.json`.  
Probe overall: RED (2 RED, 9 YELLOW, 6 GREEN).
