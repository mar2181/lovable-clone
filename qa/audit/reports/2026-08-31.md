# HS Web App Builder — Audit Report

**Date:** 2026-08-31T11:05Z · **Run:** #11 (scheduled, 3-day cadence) · **Mode:** auto-fix safe / flag risky  
**Overall: 🔴 RED** — Voice box P0 day 55+, /dashboard P0 day 82+. Build E2E unverifiable from cloud. 28 stale audit PRs. No new fixes since Aug 28.

> Probe: 6 GREEN · 9 YELLOW · 2 RED  |  Deep audit: 5 domains checked  |  Auto-healed: none (no cloud creds for pod restart)

---

## 🚩 Needs Mario (ranked P0 → P3)

### P0 — Voice Box DOWN (`/healthz → timeout/empty`) — Day 55+

`https://udcz4k7kse1zw6-7860.proxy.runpod.net/healthz` returned empty (connection timeout). Total voice outage. Pet Concierge frontend wiring is **intact** (data-backend=selfhosted → /boxapi → `next.config.ts` proxies to `https://udcz4k7kse1zw6-7860.proxy.runpod.net` — exact match). This is a pure infrastructure failure; code is correct.

**This has been RED every run since 2026-07-07 (55+ days). Urgent action needed.**

**Fix (operational — needs RUNPOD_API_KEY from local vault):**
```bash
# 1. Check pod state
curl -H "Authorization: Bearer $RUNPOD_API_KEY" https://rest.runpod.io/v1/pods/udcz4k7kse1zw6

# 2. If EXITED/stopped:
curl -X POST -H "Authorization: Bearer $RUNPOD_API_KEY" https://rest.runpod.io/v1/pods/udcz4k7kse1zw6/start

# 3. If pod RUNNING but box mute:
curl -X POST https://udcz4k7kse1zw6-7860.proxy.runpod.net/dashboard/agent/restart

# 4. Verify:
curl https://udcz4k7kse1zw6-7860.proxy.runpod.net/healthz  # → {"ok":true}
```
> ⚠️ Pod billed ~$0.69/hr (~$500/mo) even when dead. If voice is not in active use, stop the pod and restart on demand.

---

### P0 — `/dashboard` 404 in production (Vercel deploy drift) — Day 82+

`https://hswebappbuilder.space/dashboard` → HTTP 404. Route **exists** in codebase at `app/dashboard/page.tsx`. `npm run build` confirms it as a valid static route. Vercel is serving a stale deployment predating when the dashboard route was added.

**This has been RED every run since at least 2026-06-10 (82+ days). Urgent action needed.**

**Fix:**
```bash
vercel --prod   # or Vercel dashboard → Deployments → Redeploy latest master commit
```
Then verify: `curl -I https://hswebappbuilder.space/dashboard` → 200

---

### P2 — Build engine E2E unverifiable from cloud (persistent methodology gap)

The security fix (`ENVIRONMENT=production`, `DEV_BYPASS_AUTH` unset) correctly returns 401 for `Bearer dev-local-user`. Worker infra is reachable (`/health → 200`, `/api/spec.json → 200`, `/api/projects anon → 401` correct). But the SSE build pipeline cannot be tested without a valid Clerk JWT or a dedicated audit API key.

**Fix (one-time setup — eliminates blind spot permanently):**
```bash
# In worker/wrangler.toml or via wrangler secret:
wrangler secret put AUDIT_API_KEY   # generate a random secret, store in vault
# Add auth branch in worker/src/middleware/auth.ts for AUDIT_API_KEY bearer token
# Store key in cloud audit env
```
Until then: run `node qa/audit/audit-probe.mjs --deep` locally after each deploy.

---

### P2 — npm vulnerabilities: 12 (7 high, 5 moderate) · 2 Dependabot PRs unmerged

`npm audit` in root workspace: `{ high: 7, moderate: 5, low: 0, critical: 0, total: 12 }`

Two Dependabot PRs open with no action:
- **PR #28 (branch: `dependabot/npm_and_yarn/npm_and_yarn-5984bbb696`)** — open since 2026-06-20 (~72 days) — bumps packages across 3 workspace directories
- **PR (branch: `dependabot/npm_and_yarn/mcp-server/npm_and_yarn-04db377a11`)** — open since 2026-07-21 (~41 days) — bumps `fast-uri` in mcp-server

**Fix:** Review both PRs. If lock-file-only bumps, merge them. Then `npm audit` to confirm count drops.

---

### P3 — 28+ stale open audit-report PRs cluttering GitHub

28+ open `audit/report-*` PRs (PR #27 → #67). None have been merged or closed. The PR list is unusable for real work.

**Fix:**
```bash
# Close all stale audit PRs via GitHub UI: filter PRs by "audit/report" head branch → Close all
# Then delete remote branches (after closing PRs):
git fetch --prune
git branch -r | grep 'origin/audit/report' | \
  grep -v 'audit/report-2026-08-31' | \
  sed 's|origin/||' | \
  xargs -I{} git push origin --delete {}
```
Keep only this run's branch open until acknowledged.

---

### P3 — `claude/builder-buddy-rail` PR #20 (93+ days open, no action)

PR #20 docks the Space Mario assistant rail in the builder UI. "BRING IN FOR REVIEW" since Jun 7 baseline. Still open and unreviewed.

**Fix:** Review PR #20 — merge if still in scope, close/archive if superseded by other work.

---

### P3 — `middleware.ts` → `proxy.ts` rename needed (persists, every run)

`npm run build` emits a deprecation warning: `middleware` file convention deprecated in Next.js 16+. Will break proxy layer on next major upgrade.

**Fix (repo edit — flagged per policy.md):**
```bash
git mv middleware.ts proxy.ts  # update imports
npm run build                  # verify proxy routes intact
```

---

## 🔧 Auto-healed this run
**None.** No operational fix was possible without cloud creds (RUNPOD_API_KEY, CLOUDFLARE_API_KEY). Both P0s are infrastructure-level and require local vault access.

---

## ✅ Verified WORKING

| Domain | Status | Evidence |
|---|---|---|
| **Worker infra** | 🟢 GREEN | `/health → 200`, `/api/spec.json → 200` (openapi 3.1.0), `/api/projects anon → 401` (gated correctly) |
| **Frontend root** | 🟢 GREEN | `https://hswebappbuilder.space/ → 200` |
| **GitHub import route** | 🟢 GREEN | `/api/github/import anon → 401` (deployed + gated) |
| **Sandpack unit guards** | 🟢 GREEN | Alias: 5/5 PASS · Assets: 9/9 PASS |
| **Pet concierge wiring** | 🟢 GREEN | `data-backend="selfhosted"` → `data-connect-url="/boxapi"` → `next.config.ts /boxapi → https://udcz4k7kse1zw6-7860.proxy.runpod.net` (exact match). `agent_id` present. Code is correct; box is the infra failure. |
| **Security — dev-bypass-prod** | 🟢 **RESOLVED** | `GET /api/credits` with `Bearer dev-local-user` → 401. `wrangler.toml` now has `ENVIRONMENT="production"`, `DEV_BYPASS_AUTH` unset. **P1 fixed.** |
| **Security — .dev.vars** | 🟢 GREEN | `.gitignore:53` covers `worker/.dev.vars`; `git ls-files worker/.dev.vars` → empty (not tracked) |
| **Security — CORS** | 🟢 GREEN | `worker/src/index.ts` uses exact-match origin allowlist (not `*`) |

---

## 🩹 Hygiene backlog (P3 — batched)

- **28+ stale audit PRs** (see §Needs-Mario above — most urgent P3)
- **npm vulnerabilities: 12 remaining** — 2 Dependabot PRs open 41–72 days with no action
- **`middleware.ts` → `proxy.ts` rename** — deprecation warning every build
- **PR #20 builder-buddy-rail** — 93+ days open, no reviewer assigned
- **Pod billed ~$500/mo** even when dead (no idle-shutdown / on-demand broker-wake flow)
- **Telegram/SMS not configured** (`/api/share/health → telegram:undefined, sms:undefined`)
- **ESLint hygiene** — 80+ warnings (unused vars, missing useEffect deps, `<img>` vs `<Image />`)
- **`GET /api/projects/<deleted-id>`** returns 200 with `{"error":"Project not found"}` — should be 404

---

## 🗂 GitHub Reconciliation (surface only — NEVER merge)

Non-master branches:

| Branch | Age | What it is | Recommendation |
|---|---|---|---|
| **`claude/builder-buddy-rail`** (PR #20) | 93+ days | Space Mario assistant rail — builder UI right-panel | **REVIEW** — merge if in scope, close if superseded |
| **`dependabot/npm_and_yarn/npm_and_yarn-5984bbb696`** | 72 days | Lock-file bump, 3 workspace dirs | **MERGE** — likely safe lock-file-only change |
| **`dependabot/npm_and_yarn/mcp-server/npm_and_yarn-04db377a11`** | 41 days | Bumps `fast-uri` in mcp-server | **MERGE** — likely safe |
| **`audit/report-*` (28 branches)** | 3–82 days | Audit report archives | **ARCHIVE (delete)** — PRs served notification purpose; close PRs and delete branches |

---

## Δ vs 2026-08-28 (run #10)

| Item | Direction | Detail |
|---|---|---|
| Voice box P0 | 🔴 PERSISTS | Day 55+ (was 52+). No change. |
| /dashboard 404 P0 | 🔴 PERSISTS | Day 82+ (was 79+). No change. |
| npm vulnerabilities | 🔴 SAME | 12 total (7 high, 5 moderate). Both Dependabot PRs still open. |
| Stale audit PRs | 🔴 WORSE | 28 open (was 25). Three more audit PRs added (Aug 22, 25, 28). |
| Build E2E | ⚪ UNVERIFIABLE | Same as last 3 runs — needs local run with Clerk JWT or AUDIT_API_KEY setup |
| P1 dev-bypass-prod | ✅ CONFIRMED RESOLVED | `Bearer dev-local-user → 401`. `ENVIRONMENT=production` confirmed in wrangler.toml. |
| Sandpack unit guards | ✅ PASS | Both 5/5 and 9/9 — no regression |
| Pet concierge wiring | ✅ INTACT | Code is correct; pure infra failure |

---

## ⛔ Checks skipped (no cloud creds)

| Check | Reason skipped | What to run locally |
|---|---|---|
| **RunPod pod-power** | `RUNPOD_API_KEY` not in cloud env | `curl -H "Authorization: Bearer $RUNPOD_API_KEY" https://rest.runpod.io/v1/pods/udcz4k7kse1zw6` |
| **Vercel deploy-drift** | `VERCEL_TOKEN` not in cloud env | `vercel ls` or Vercel dashboard — confirm latest master commit is deployed |
| **Worker auto-redeploy self-heal** | `CLOUDFLARE_API_KEY`+email not in cloud | Run `node qa/audit/selfheal.mjs` locally (not needed this run — worker is up) |
| **Box `POST /api/connect` session test** | RunPod broker token in `.dev.vars` | Test HMAC-gated broker locally: `POST <box>/api/connect` → confirm `connectUrl`+token returned |
| **Build E2E (`POST /api/build` SSE)** | `Bearer dev-local-user` correctly 401 in prod; no AUDIT_API_KEY configured | Run `node qa/audit/audit-probe.mjs --deep` locally with valid Clerk JWT, or set up AUDIT_API_KEY |
| **Telegram notification** | No Telegram token in cloud | `notify_mario()` / `POST /api/share/phone` — run locally |

---

## How this was produced

Tier-1 probe (`node qa/audit/audit-probe.mjs --json`) + 5 deep domain checks (build auth blocked — noted).
Probe JSON saved at `qa/audit/reports/latest-probe.json` by probe.
Probe overall: RED (2 RED · 9 YELLOW · 6 GREEN).
